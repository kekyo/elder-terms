# libcurlによるFTPS対応計画

作成日: 2026-09-10

調査対象: 現在のコードベース、実装基準コミット `1bf871f`（`chore: finalize libcurl FTP packaging and validation`）。追加調査の開始時点は `01f630a`。

更新日: 2026-09-10。ユーザーの追加指定により、TLSバージョン・旧TLS互換設定の選択と、証明書検証失敗時の「拒否／オーバーレイで確認」を計画に追加した。従来の「TLS 1.2以上に固定」「証明書エラーは常に拒否」という条件は、以下の選択可能な仕様に置き換える。

状態: ユーザーの実装指示を受け、段階1〜5が完了し、段階6を実施中。明示的FTPSと暗黙的FTPSの両方を対象とする。

## 1. 対応方針

既存のFTPバックエンドを拡張し、FTP接続の設定で暗号化方式を選べるようにする。接続種別は `[general] type=ftp` のまま、FTPタブに「暗号化」を追加する。

| 選択肢 | 保存値 `[ftp] tls_mode` | 組込み既定ポート | 接続方法 |
| --- | --- | --- | --- |
| FTP（暗号化なし） | `none` | 21 | 現在の平文FTP |
| FTPS（明示的TLS） | `explicit` | 21 | FTP接続後にTLSへ切り替えてから認証 |
| FTPS（暗黙的TLS） | `implicit` | 990 | TCP接続直後からTLSで通信 |

方式の違いとポートの根拠はcurl公式の[FTPS解説](https://everything.curl.dev/ftp/ftps.html)に従う。設定未指定時は現在のFTP動作を維持する。FTPSを選んだ場合、TLSの確立に失敗した接続では認証・転送を続行しない。証明書検証の失敗だけは、設定と利用者の明示的な許可により、その接続中の例外として扱える。

- FTPSでは制御接続と、一覧取得・アップロード・ダウンロードに使う全データ接続を暗号化する。明示的FTPSでは、接続開始時の挨拶とTLS切替要求は平文になるが、ユーザー名・パスワードはTLS確立後に送る。
- サーバー証明書の信頼チェーン、有効期間、接続先名をまず検証する。検証失敗時は「拒否」を既定とし、「オーバーレイで確認」を選んだ接続では利用者が許可・拒否を判断する。
- TLSバージョンは最小・最大を選択可能にする。既定は1.2以上とし、明示的な設定で1.0／1.1を許容する。TLSのバージョン、暗号スイート、明示的FTPSのAUTHコマンドの優先順、OpenSSLの旧TLS互換設定を独立して扱う。
- Passiveを既定とし、現在のActive、IPv4／IPv6、EPSV／PASV、EPRT／PORTも検証対象に含める。
- ログイン時のユーザー名・パスワード入力、パスワードを保存しない動作、2ペインブラウザー、FIFOでの操作直列化、有界バッファ、キャンセルと非同期終了を引き継ぐ。
- 今回の範囲はFTPへのTLS追加と上記の接続設定。クライアント証明書認証、証明書を最初から無条件に許可する設定、例外の恒久保存、利用者が指定する公開鍵ピン設定、プロキシ設定、IP scanへの990番ポート追加は対象外とする。実行中の例外を許可した証明書へ限定するための内部照合は今回の実装に含める。
- 既存のC++20／GTK3／Meson、npm／Vitest／gestamentを使用する。ビルド方式の移行、FTPの再実装、外部コードの変更は含めない。

## 2. 現在の実装と変更箇所

| 現在の実装 | 計画への影響 |
| --- | --- |
| `shared/include/elder-terms/settings/ftp-settings.h` と `shared/src/settings/ftp-settings.cpp` は平文FTPの接続情報を定義する。ポート既定は21。 | TLS方式、バージョン範囲、暗号・互換設定、証明書エラー方針、CAファイル、方式に応じた実効ポートを追加する。公開型・関数のDoxygenコメントも更新する。 |
| `shared/src/settings/settings-store.cpp` は不正値を警告し、その値を採用せず既定値・継承値を使用する。 | TLS方式の不正値が `none` に変わらないよう、接続を阻止できる検証結果を保持する必要がある。 |
| `shared/src/settings-widget/settings-widget.cpp` の `create_ftp_page()` と同期・変更処理は、アドレス、ポート、ユーザー名、データ接続方式、初期ディレクトリを扱う。実行中の接続先設定は読取り専用。 | 同じ継承・適用・取消の仕組みにTLS設定を追加する。 |
| `elder-terms-vte/src/ftp/ftp-client.cpp` の `endpoint_url()` はURLのschemeを `ftp` に固定する。 | 暗黙的FTPSだけ `ftps` にする。アドレスのエスケープ、IPv6、パス構築は既存処理を使う。 |
| `elder-terms-vte/src/ftp/curl-ftp-session.cpp` は操作ごとに `curl_easy_reset()` し、`CURLOPT_PROTOCOLS_STR="ftp"`、`CURLOPT_USE_SSL=CURLUSESSL_NONE` を設定する。 | 各操作で接続設定からTLSオプションを再適用する。初回ログインだけの変更では足りない。 |
| 同セッションはlibcurl 7.88.1以上、非同期DNS、FTP機能を実行時に確認する。 | FTPS選択時にはTLS機能と必要なプロトコルも確認する。 |
| `elder-terms-vte/src/file-transfer/file-transfer-main.cpp` はFTPの認証、タイトル、開始失敗表示、worker終了待機を扱う。SFTPは `prompt_file_transfer_window_async()` でホスト鍵確認を表示する。 | FTP／FTPSの表示を選択方式に合わせる。同じオーバーレイ基盤で証明書確認を表示し、判定はFTP専用の非同期応答としてクライアントへ渡す。 |
| `elder-terms-vte/tests/cpp/ftp-test-server.cpp` は自前の平文FTP fixture。client、stream、lifetime、runtime-capabilitiesのC++テストと `ftp-live-window.test.ts` がある。 | fixtureへ実TLSを追加し、既存の実転送シナリオを拡張できる。 |
| `elder-terms-vte/meson.build` はlibcurl 7.88.1以上を要求し、`prereq.sh` は `libcurl4-openssl-dev` と `ca-certificates` を含む。配布パッケージの明示的実行時依存には `ca-certificates` がない。 | 最低libcurlバージョンを維持して検証する。配布先のCAストアもパッケージ検証に含める。 |

調査環境ではpkg-configがlibcurl 8.5.0、OpenSSL 3.0.13を報告し、curl-configはAsynchDNS、SSL、FTP、FTPSを報告した。これは開発環境の調査結果であり、最低対応版や配布環境での動作確認の代わりにはしない。

## 3. 設定・接続・証明書の詳細

### 設定形式と継承

```ini
[general]
type=ftp

[ftp]
address=ftp.example.com
tls_mode=explicit
username=alice
data_connection_mode=passive
local_directory=
remote_directory=.
```

独自CAを使う場合は `[ftp] ca_file=/absolute/path/company-ca.pem` を追加する。暗黙的FTPSは `tls_mode=implicit`。独自ポートは現在と同じ `port=2121` の形式で指定する。

- `tls_mode` の有効値は `none`、`explicit`、`implicit`。組込み既定は `none`。未指定はグローバル設定を継承する。
- 以下の追加設定もグローバルからの継承・接続ごとの明示指定・既定への復帰を扱う。読み込んだ不正値を黙って別のTLS条件へ変更しない。初期段階でまだ未対応の非既定設定を指定した場合も、未対応を通知して接続を止める。
- ポートは接続設定、明示されたグローバル設定、TLS方式に対応する組込み既定値の順で解決する。現在の `SettingValueSource` を利用して、保存された21と組込み既定の21を区別する。
- TLS方式を変更した際、組込み既定を使用中の場合だけ実効ポートが21／990に追従する。ユーザーが明示した21、990、独自ポートやグローバルの指定値は上書きしない。ポートを空に戻した場合の表示も同じ解決処理を使用する。
- `ca_file` 未指定は継承、明示的な空文字列はシステムCA、絶対パスは指定CAバンドルとする。グローバルに独自CAがあっても、接続単位でシステムCAを選べるようにする。
- 指定CAファイルはPEM形式のCAバンドルとし、絶対パスで保存する。相対パスやシェル展開に依存した解決は行わない。存在しない・読めない・不正なファイルでは接続を失敗させる。
- `ca_file` が空ならlibcurlの組込みCA設定を使い、指定がある場合は `CURLOPT_CAINFO` に渡す。既定CAの場所はlibcurlのビルド設定によるため、パスをアプリに固定しない。独自バンドルの指定を「このCAだけを信頼する」という制限機能とは定義しない。根拠: [CURLOPT_CAINFO](https://curl.se/libcurl/c/CURLOPT_CAINFO.html)。
- 不明・空・壊れた `tls_mode` は、未指定と区別して接続エラーにする。読込み段階でキー・出所を持つ構造化検証結果を残し、コピー、継承、起動用設定の上書きにも反映する。下位設定の不正値を有効な上位指定が置き換えた場合はそのキーのエラーを解消する。警告文字列の検索や `none` へのフォールバックで処理しない。
- この検証結果は設定UIで修正できるように扱い、FTP起動時にはネットワーク接続前に確認する。他プロトコルや既存項目の不正値処理を一律に変更しない。

### TLSバージョンとバリエーション

対象機器はまだ指定されていないため、バリエーションは明示的／暗黙的FTPS、AUTHの優先順、TLSバージョン範囲、暗号スイート、旧TLSに必要なOpenSSLの許容条件として具体化する。

| `[ftp]` の設定 | 値と組込み既定 | 意味 |
| --- | --- | --- |
| `tls_min_version` | `1.0`／`1.1`／`1.2`／`1.3`、既定 `1.2` | 許容する最小バージョン |
| `tls_max_version` | `default`／`1.0`／`1.1`／`1.2`／`1.3`、既定 `default` | 最大バージョン。`default` はTLSバックエンドの上限 |
| `tls_auth_order` | `tls`／`ssl`／`default`、既定 `tls` | 明示的FTPSでAUTH TLS優先、AUTH SSL優先、libcurlに委任 |
| `tls_compatibility` | `standard`／`openssl_legacy`、既定 `standard` | 通常の暗号・署名条件、またはOpenSSLの旧TLS互換条件 |
| `tls_cipher_list` | 文字列、既定は空 | OpenSSL構文によるTLS 1.2以下の暗号スイート指定。空は既定 |
| `tls13_cipher_list` | コロン区切りの文字列、既定は空 | TLS 1.3の暗号スイート指定。空は既定 |
| `certificate_error_action` | `reject`／`prompt`、既定 `reject` | 証明書検証失敗を拒否するか、オーバーレイで利用者に確認するか |

最小と最大を同じ値にすれば、そのバージョンだけに固定できる。例えばTLS 1.2のみの接続は両方を `1.2` にする。数値の順序を検証し、最小が最大を超える設定、未知の値、SSLv2／SSLv3指定を接続前に拒否する。設定の上限を超えるバージョンや、下限未満のバージョンへの自動再試行は行わない。

旧TLS 1.0だけを許容する接続の例:

```ini
[ftp]
tls_mode=explicit
tls_min_version=1.0
tls_max_version=1.0
tls_compatibility=openssl_legacy
certificate_error_action=reject
```

- libcurlの最小値指定は通常「その版以上」であり、固定には最大値も必要。[CURLOPT_SSLVERSION](https://curl.se/libcurl/c/CURLOPT_SSLVERSION.html) の最小値と `CURL_SSLVERSION_MAX_*` を組み合わせる。
- TLS 1.0／1.1はlibcurlの設定APIから選択できる。ただし、実際の利用可否はTLSバックエンドのビルドとポリシーに依存する。SSL機能の有無だけでは各バージョンの利用可能性は確定しない。
- OpenSSL 3では古いTLSの署名方式が通常のセキュリティレベルで拒否される場合がある。`openssl_legacy` では当該セッションの暗号設定に `@SECLEVEL=0` を明示適用する。通常設定から失敗を検出して自動で切り替えない。根拠: [OpenSSLのセキュリティレベル](https://docs.openssl.org/3.0/man3/SSL_CTX_set_security_level/)、[暗号リスト構文](https://docs.openssl.org/3.0/man1/openssl-ciphers/)。
- 旧TLS互換は暗号・鍵長・署名の許容条件を緩める設定であり、証明書検証失敗の自動許可とは別にする。証明書チェーン・期限・名前の検証と `certificate_error_action` は旧TLS互換でも適用する。
- `tls_compatibility` の変更はバージョン範囲を自動変更しない。旧TLSを使う場合は範囲も明示する。システムの `openssl.cnf`、プロセス全体の環境変数、他ウィンドウのTLS条件を変更しない。
- 標準かつ暗号リスト未指定ならlibcurlの既定を維持する。旧TLS互換でリストが空の場合は `DEFAULT:!aNULL:!eNULL:@SECLEVEL=0` を使用する。独自のTLS 1.2以下のリストにも `!aNULL:!eNULL` を適用し、無認証・無暗号の接続を許可しない。利用者の暗号文字列に `@SECLEVEL` を含めることは拒否し、セキュリティレベルの変更は互換設定に集約する。
- 暗号リストはTLS 1.2以下と1.3を別々に指定する。片方を指定しても、もう片方は無効にならない。libcurlは一部の未知の暗号名を無視し、有効な候補が残れば受理するため、全項目が厳密に認識されたとは表示しない。使用可能な候補がない場合はエラーにする。根拠: [curlの暗号スイート指定](https://curl.se/docs/ssl-ciphers.html)、[CURLOPT_SSL_CIPHER_LIST](https://curl.se/libcurl/c/CURLOPT_SSL_CIPHER_LIST.html)、[CURLOPT_TLS13_CIPHERS](https://curl.se/libcurl/c/CURLOPT_TLS13_CIPHERS.html)。
- 初期の配布保証は既存のOpenSSL版libcurlに合わせる。旧TLS互換と上記の暗号リスト構文はOpenSSL固有として検査し、GnuTLS等へ同じ文字列を渡さない。他バックエンドでこの設定を指定した場合は未対応として通知し、黙って無視しない。
- `AUTH SSL` はFTPのコマンド名であり、SSLv3を使う指定ではない。選択するのはコマンドの優先順で、TLSのバージョンとは独立する。片方だけを送る設定はlibcurlのこのAPIにはない。根拠: [CURLOPT_FTPSSLAUTH](https://curl.se/libcurl/c/CURLOPT_FTPSSLAUTH.html)。

旧プロトコル対応の境界:

| 対象 | 検討結果 |
| --- | --- |
| TLS 1.2／1.3 | バージョン範囲・固定と暗号スイートを設定可能 |
| TLS 1.0／1.1 | 明示的な旧TLS設定で対応する。開発環境で実FTPSの制御・データ接続を確認済み。配布対象ごとの検証が必要 |
| SSLv2／SSLv3 | 現行要件のlibcurlでは対応不可。libcurl 7.77.0以降は完全拒否するため、セキュリティレベルを下げても復活しない。[公式仕様](https://curl.se/libcurl/c/CURLOPT_SSLVERSION.html) |
| RC4／3DES等だけを使う相手 | 実装済み暗号とビルド条件に依存する。開発環境ではlegacy providerを追加しても対象TLSスイートが列挙されなかったため、対応を保証しない。[OpenSSL暗号一覧の仕様](https://docs.openssl.org/3.0/man1/openssl-ciphers/) |
| 安全な再ネゴシエーションに非対応の相手 | TLS 1.0対応とは別の問題。OpenSSL固有の追加動作が必要で、汎用の旧TLS互換設定には含めない。対象機器が判明した場合に別途検討する。[OpenSSLの再ネゴシエーション仕様](https://docs.openssl.org/3.0/man3/SSL_CTX_set_options/) |

### 証明書検証失敗時のオーバーレイ

`certificate_error_action=reject` では証明書検証失敗をエラーとして表示して終了し、許可ボタンを表示しない。`prompt` では既存のファイル転送ウィンドウ内に確認オーバーレイを表示し、「この接続中は許可」「接続を中止」を選べるようにする。正常な証明書では、どちらの設定でも確認を表示しない。

- オーバーレイには、接続先のホスト名・ポート、制御／データ接続の別、失敗理由、証明書の主体・発行者・有効期間・SHA-256フィンガープリントを表示する。取得できない項目は未取得と表示し、証明書由来の文字列をGTKのマークアップとして解釈しない。
- 自己署名・未信頼CA、期限切れ／有効期間前、名前不一致などの証明書検証失敗を確認対象とする。共通バージョン／暗号がない、TLS交渉自体の失敗、設定やCAファイルの読込み失敗は証明書許可で解消する問題ではなく、通常の接続エラーにする。
- 許可は利用者が明示操作した時だけ成立する。初期フォーカスは中止側とし、Esc、ウィンドウを閉じる、接続のキャンセルは拒否として扱う。認証パネルで押したEnterを引き継いで許可しない。
- 例外はその論理接続セッションのメモリーだけに保持する。対象は接続先ホスト名・制御ポート・TLS方式／設定と、提示されたサーバー証明書のSHA-256、許可した検証失敗の組合せ。証明書変更や新たな失敗は再確認する。同一証明書・同一失敗のデータ接続では繰り返し確認しない。
- 設定ファイルには `prompt`／`reject` の方針だけを保存する。例外をCAストア・設定ファイルへ永続化しない。アプリ再起動、ウィンドウの新規接続、手動再接続では例外を破棄する。同じ論理セッション内のlibcurlによる張り直しは同じ証明書と失敗に限って許可を使用する。
- 制御接続では、未承認の証明書に対してユーザー名・パスワードやファイル操作を送信しない。データ接続でも未承認の証明書にはファイル内容を送らず、受信内容を転送成功として採用しない。制御接続で許可したからといって異なるデータ接続証明書を無条件に受理しない。
- 許可後もTLSバージョン、暗号スイート、制御・データの暗号化必須条件は変えない。旧TLS互換の選択と証明書の許可はそれぞれ独立して必要となる。
- 証明書の例外で接続した場合は、その状態を接続画面で確認できるようにする。通常の検証に成功した接続として表示しない。

実装方針:

1. 証明書検証で停止した結果に、文字列解析に依存しない検証エラーと証明書の複製データを付ける。`CURLOPT_SSL_CTX_FUNCTION` とOpenSSLの公開検証APIを使う小さな内部コンポーネントで扱い、TLSオブジェクトの借用ポインターをworker外へ渡さない。`CURLOPT_CERTINFO` だけで失敗時にも情報が揃うとは仮定しない。
2. 失敗したハンドシェイクを終了してから、呼出し側のcardio非同期処理でオーバーレイの応答を待つ。UIの回答待ちを同期的なTLS検証コールバックへ閉じ込めず、人の判断時間をネットワークの接続期限から分ける。既存の最低版7.88.1で使用できない新しいlibcurlの非同期証明書検証機能には依存しない。
3. 許可後は新しいハンドシェイクで実際の証明書と例外の対象を照合し、一致した検証失敗だけを許容する。名前不一致の例外にも対応しつつ、未許可の証明書・ホストに対する名前検証を省略しない。IP SAN・IDN・ワイルドカードを含む名前の照合条件を通常のlibcurl経路と揃え、例外経路だけが未承認の名前を受理しないことを検証する。正常時の検証はライブラリに委譲し、アプリ独自の証明書チェーン検証を実装しない。
4. 自動再試行は初回認証前など、FTPの副作用が始まっていないと確認できる操作に限定する。データ接続の失敗では既に `STOR` 等が送信されている可能性があるため、許可だけで変更操作を再送しない。その操作の失敗・部分ファイル状態を通知する。現在のクライアントはTLSエラーで失敗状態になるため、許可後は必要に応じて同じ論理セッションの例外を保持してクライアントを再作成し、認証とブラウザーの利用を復帰させる。失敗した変更操作の再実行は利用者の操作に委ね、二重作成・二重転送を生じる再試行はしない。
5. 証明書確認中もウィンドウ終了・キャンセルで待機処理を解放する。閉じたウィンドウへ遅延した応答を適用しない。複数ウィンドウの許可状態と確認要求は分離する。

この機能により、当初の「アプリ本体はOpenSSLへ直接依存しない」という設計を変更する。配布用ファイル転送実行ファイルの内部証明書コンポーネントにOpenSSL依存を限定し、公開のFTP APIへOpenSSL型を露出させない。実行時のlibcurlが使用するOpenSSLと直接リンクしたライブラリの整合を検査する。他バックエンドで `prompt` が実装できない場合は、設定を無視せず未対応として接続を止める。`reject` は標準のlibcurl検証経路を使用できる。

APIの根拠と制約: [CURLOPT_SSL_CTX_FUNCTION](https://curl.se/libcurl/c/CURLOPT_SSL_CTX_FUNCTION.html)、[OpenSSLの検証コールバック](https://docs.openssl.org/3.0/man3/SSL_CTX_set_verify/)、[証明書検証処理の拡張](https://docs.openssl.org/3.0/man3/SSL_CTX_set_cert_verify_callback/)、[接続先名の検証](https://docs.openssl.org/3.0/man3/X509_check_host/)。libcurlの接続再利用ではコールバックが再実行されないことがあるため、異なる例外やTLS設定を持つ接続プールを共有しない。

### libcurlへの適用

| 設定 | 平文FTP | 明示的FTPS | 暗黙的FTPS |
| --- | --- | --- | --- |
| URL scheme／`CURLOPT_PROTOCOLS_STR` | `ftp`／`ftp` | `ftp`／`ftp` | `ftps`／`ftps` |
| `CURLOPT_USE_SSL` | `CURLUSESSL_NONE` | `CURLUSESSL_ALL` | `CURLUSESSL_ALL` |
| `CURLOPT_FTPSSLAUTH` | 適用不要 | `tls_auth_order` に対応する値 | 適用不要 |
| `CURLOPT_SSL_VERIFYPEER` | TLS未使用 | 通常 `1L`、承認済み例外は上記の限定検証経路 | 同左 |
| `CURLOPT_SSL_VERIFYHOST` | TLS未使用 | 通常 `2L`、承認済み名前不一致は上記の限定検証経路 | 同左 |
| `CURLOPT_SSLVERSION` | TLS未使用 | 選択した最小値と最大値の組合せ | 同左 |
| `CURLOPT_SSL_CIPHER_LIST` | TLS未使用 | TLS 1.2以下の暗号設定と互換設定から構成 | 同左 |
| `CURLOPT_TLS13_CIPHERS` | TLS未使用 | 指定したTLS 1.3用リスト | 同左 |

- `CURLUSESSL_ALL` で制御・データ双方の保護を要求する。`CURLUSESSL_TRY` や制御接続だけを保護する方式は使用しない。`AUTH`、`PBSZ`、`PROT`の手動送信は追加せずlibcurlに任せる。根拠: [CURLOPT_USE_SSL](https://curl.se/libcurl/c/CURLOPT_USE_SSL.html)。
- `CURLFTPAUTH_TLS` は `AUTH TLS` を優先する指定であり、失敗時の `AUTH SSL` 試行を禁止する指定ではない。最低TLSバージョンの制約は別に適用する。両AUTH要求を拒否された場合には認証しない。根拠: [CURLOPT_FTPSSLAUTH](https://curl.se/libcurl/c/CURLOPT_FTPSSLAUTH.html)、[CURLOPT_SSLVERSION](https://curl.se/libcurl/c/CURLOPT_SSLVERSION.html)。
- 通常は信頼チェーン検証と接続先名検証の両方を有効にする。URLには設定されたホスト名を保持し、DNS解決した数値IPへの置換でSNIや名前検証を変えない。IP指定の正常な名前検証には一致するIP SANが必要。承認済み例外は上記の対象に限定する。根拠: [CURLOPT_SSL_VERIFYPEER](https://curl.se/libcurl/c/CURLOPT_SSL_VERIFYPEER.html)、[CURLOPT_SSL_VERIFYHOST](https://curl.se/libcurl/c/CURLOPT_SSL_VERIFYHOST.html)。
- `CURLOPT_FTP_SSL_CCC` は無効のままとし、認証後も制御接続を暗号化する。根拠: [CURLOPT_FTP_SSL_CCC](https://curl.se/libcurl/c/CURLOPT_FTP_SSL_CCC.html)。
- TLSオプションはworker内で `curl_easy_reset()` 後に毎回設定し、戻り値を確認する。現在の最低版にも対応するようenum値の `long` キャストを保つ。
- 1ウィンドウの接続設定をセッション中は固定する。新しい設定で接続し直す場合は新しいセッションを作る。`curl_easy_reset()` は既存接続とTLSセッションキャッシュを消去しないため、リセットを設定変更後の再認証手段として扱わない。根拠: [curl_easy_reset](https://curl.se/libcurl/c/curl_easy_reset.html)。
- 既定のTLSセッションキャッシュを維持する。データ接続でセッション再利用を要求するサーバーとの相互接続は実際に検証する。根拠: [CURLOPT_SSL_SESSIONID_CACHE](https://curl.se/libcurl/c/CURLOPT_SSL_SESSIONID_CACHE.html)。
- 実行時能力検査は既存の `feature_names`／`protocols` を使う。平文FTPにTLSを要求せず、明示的FTPSではFTP＋SSL、暗黙的FTPSではFTPS＋SSLを追加確認する。API利用前に既存の版・構造体世代チェックを行う。根拠: [curl_version_info](https://curl.se/libcurl/c/curl_version_info.html)。
- libcurl処理は現在の専用workerと非同期adapterで実行する。TLS処理のための追加workerや、GTK側のポーリング待機は導入しない。

libcurlオプションは公式文書と、導入済み `/usr/include/x86_64-linux-gnu/curl/curl.h` のAPIコメントを調査済み。実装時に追加する外部APIについても公式文書と対象環境のコメントを確認し、非推奨・内部APIは使用しない。

### UIと失敗時の動作

- 暗号化方式は既存の継承可能な選択UIに組み込む。FTPのデータ接続方式とは別項目として表示する。最小・最大TLSバージョン、AUTH優先順、互換設定、暗号リストはTLS詳細設定として表示する。既定を維持する利用者は詳細を入力する必要がない。
- 証明書エラー方針は「拒否」「オーバーレイで確認」から選択でき、接続ごとの画面では継承も扱う。旧TLS互換の表示には暗号・署名の許容条件も変わることを添える。
- CA設定は接続画面で「継承」「システムのCA」「指定ファイル」、グローバル画面では後者の2択とする。「指定ファイル」では入力欄とファイル選択を表示し、未入力では保存・適用できない。
- 平文FTPではTLS詳細・CA・証明書エラー方針を無効表示する。FTPSに戻した際には編集中の値を復元する。暗黙的FTPSではAUTH優先順を無効表示する。実行中のウィンドウからはこれらの接続設定を変更できないが、設定された `prompt` に対するその接続中の許可判断は行える。
- FTPS選択時のウィンドウと認証・接続失敗表示にFTPSを使用する。接続前の方式表示を、証明書の検証に成功した表示として扱わない。
- 設定エラー、CAファイルの読込み失敗、証明書検証失敗、TLS交渉失敗、ログイン拒否を区別して通知する。libcurlのエラーコードを判断に使い、英語のエラーテキストを解析しない。同じコードに集約される証明書問題は検証失敗としてまとめ、詳細メッセージを添える。
- 証明書の許可ボタンは `prompt` の確認オーバーレイだけに表示する。`reject` の接続エラー画面や、TLS交渉自体の失敗には表示しない。パスワードや秘密情報を診断ログへ追加しない。

## 4. テスト方針と共通の進め方

各段階で期待動作のテストを先に追加し、ルートの `npm run test` を全体実行してREDを確認する。その段階の実装後、同じ全体実行でGREENを確認してコミットする。未実装の新APIを参照してコンパイルを壊すことをREDの代用にせず、可能な限りINI入力、既存クライアントAPI、実ウィンドウの操作から不足する動作を再現する。

- ビルドと全体テストは同じビルドディレクトリで重複起動しない。既存のルートテストがC++、共有設定UI、ランチャー、端末・ファイル転送を順に検証する流れを使用する。
- 自前のC++ FTP fixtureへOpenSSLによるTLS transportを合成し、既存のコマンド処理・ファイル操作を使用する。アプリ本体の通信はlibcurlへ委譲し、OpenSSLを直接使う本体処理は証明書確認の内部コンポーネントに限定する。
- テストfixtureは制御・データ接続それぞれのTLS確立と実際の暗号化I/Oを確認できるようにする。`PROT P` がログにあることだけではデータの保護を検証済みとしない。テストサーバーの鍵や証明書は製品へ同梱しない。
- テスト用CAと証明書は一時ディレクトリで生成する。正常、有効期限切れ、未信頼、名前不一致などの証明書を用意し、期限切れになるのを待つテストや外部の公開FTPサービスに依存しない。生成・起動の補助スクリプトが必要ならNode.jsを使用する。
- `libssl-dev`／`openssl` を段階1でMesonと `prereq.sh` のビルド・テスト前提へ明示する。証明書確認を実装する段階4では、本体の証明書コンポーネントもOpenSSLへリンクする。必要なパッケージやPodmanイメージが不足する場合は具体名を示してインストールを促す。未実行の必須検証を完了扱いにしない。
- fixtureのREADY、TLS確立、データ接続待機、最終応答待機、切断完了のイベントを同期点にする。経過時間だけで「キャンセルできた」「平文を送っていない」と判断しない。
- UIの応答性とキャンセル・ウィンドウ終了は既存のgestament動画検証を拡張して確認する。画像マスターの追加・変更時は画像を目視する。
- TLSの接続・転送結果、保存した設定を再読込みした動作、実ファイルの内容を検証する。ソースコード中のオプション名や証明書ファイルの文字列だけを調べるテストにはしない。
- 新しい失敗が出た場合は計画との整合と原因を見直し、不要になった暫定変更を戻す。各段階の変更対象は以下の成果物に必要な範囲とする。

## 5. 段階1: 設定ファイルから明示的FTPSを使えるようにする

成果物: INIで `tls_mode=explicit` を指定し、実際のファイル転送ウィンドウで暗号化された一覧取得・送受信ができる。

1. fixtureの明示的TLS、証明書生成、TLSイベントを整備し、既存API／INIから呼ぶ成功・失敗テストを追加する。全体テストで未対応のREDを確認する。
2. `tls_mode`、`ca_file`、不正TLS設定を接続エラーにする読込み経路を追加する。保存・継承・再読込みまで同じ段階で扱う。この段階では `implicit` を指定した接続は未対応として拒否し、段階2の実装完了まで平文接続や明示的FTPSで代用しない。
3. セッションに `CURLUSESSL_ALL`、証明書検証、既定のTLS 1.2以上、CA指定、実行時能力確認を追加する。既存workerの停止・失敗時cleanupを使う。この段階では証明書エラーは `reject` の動作とする。
4. ファイル転送ウィンドウに設定を渡し、FTPSのタイトルと認証・失敗表示を追加する。INIからの起動とPassiveでの実転送を観測可能にする。
5. READMEのFTPS未対応記述と設定例を、この段階で利用可能な明示的FTPSの説明に更新する。英語・日本語と変更した公開コメントを揃える。

完了条件:

- [x] 明示的FTPSでログイン、一覧、バイナリの送受信、ディレクトリ作成、名前変更、削除が成功する。
- [x] 正しい独自CAでは成功し、既定の `reject` で未信頼・期限切れ・名前不一致・壊れたCAファイルでは失敗する。
- [x] `AUTH TLS`／`AUTH SSL`拒否、`PROT P`拒否、データ接続のTLS失敗で平文の認証・転送へ移行しない。
- [x] 不正なTLS方式と、この段階では未対応の暗黙的FTPSはネットワーク接続前に拒否され、設定の保存・継承・起動用上書きでも安全性を維持する。
- [x] 連続する一覧・転送、制御接続が張り直される場合にもTLS設定を適用する。TLS能力のない実行時環境ではFTPSだけを拒否する。
- [x] 実ウィンドウから操作でき、既存の平文FTPを含むルート全体テストが成功する。

コミット例: `feat: support explicit FTPS with certificate verification`

## 6. 段階2: 暗黙的FTPSとデータ接続・終了処理を完成させる

成果物: 両FTPS方式をINIから選択でき、Active／Passiveを含む転送とキャンセルが安定して動作する。

1. fixtureへ暗黙的TLSを追加し、最初の受信がTLSであることを確認するテストと実効ポートのテストを先にRED確認する。
2. `ftps://` のURLと許可プロトコル、FTPSの能力検査、組込みポート990の解決を追加する。明示ポートとグローバル継承を保持する。
3. 両方式のActive／Passive、IPv4／IPv6、EPSV／PASV・EPRT／PORTの既存代替動作、MLSD／LISTを検証する。データ接続先を制御接続の相手に制限する現在の方針も確認する。
4. TLS接続中・データハンドシェイク中・送受信中・最終応答待ち・終了時の切断を検証する。中断・短い受信・最終応答エラーを成功にしない。部分ファイル処理は既存の転送方針に従う。
5. セッション再利用を要求するfixtureケースで、一覧後の複数転送を検証する。TLS 1.2と1.3、両方式に対応したREADMEへ更新する。

完了条件:

- [x] 暗黙的FTPSは接続直後からTLSを使用し、両方式で実ファイルの一覧・送受信・変更操作が成功する。
- [x] 組込みポートは21／990に追従し、明示値21・990・独自ポートとグローバルの指定値を変更しない。
- [x] 両方式×Active／Passive×IPv4／IPv6で実転送が成功し、IPv4の従来コマンドへの代替も動作する。
- [x] TLS 1.2／1.3と、データ接続でのTLSセッション再利用要求を満たす接続が成功する。既定設定ではTLS 1.1以下だけを許す相手との接続は拒否する。旧TLSの明示許可は次の段階で実装する。
- [x] TLS処理中でもUIの応答性、キャンセル、ウィンドウ終了を維持し、全workerと待機操作の終了を確認できる。
- [x] TLS切断と最終応答の異常を転送成功として扱わず、ルート全体テストが成功する。

コミット例: `feat: support implicit FTPS and verify transfer lifecycle`

## 7. 段階3: TLSバージョンと旧TLS互換設定を選べるようにする

成果物: INIからバージョン範囲・固定、AUTH優先順、暗号スイート、旧TLS互換を指定し、古いTLSサーバーとの実転送を観測できる。

1. fixtureへバージョン・暗号スイートを固定する設定、AUTH TLSのみ／AUTH SSLのみを受け付ける設定を追加する。TLS 1.0／1.1の正常ケースも、サーバー側で必要な互換条件を明示する。追加INI設定から起動するテストで全体REDを確認する。
2. `tls_min_version`、`tls_max_version`、`tls_auth_order`、`tls_compatibility`、2種類の暗号リストを設定モデルと各操作のlibcurl設定へ追加する。バージョン範囲の検証と、実行時バックエンドの能力差も同時に扱う。
3. 標準設定と旧TLS互換の接続を同じプロセスで開き、一方の許容条件が他方へ漏れないことを確認する。再接続・連続転送にも同じ範囲と暗号条件を適用する。
4. TLS 1.0／1.1では、明示的・暗黙的FTPS、Active・Passive、送受信と最終応答まで検証する。バージョン文字列だけでなく、サーバー側で制御・データ両方の実際のTLSバージョンと暗号を確認する。
5. READMEに設定例と対応限界を記載し、既定設定はTLS 1.2以上、証明書エラーはまだ `reject` であることを揃える。

完了条件:

- [x] TLS 1.0／1.1／1.2／1.3の固定と、1.0～1.2、1.2以上などの範囲が動作し、範囲外・逆転した範囲を許可しない。
- [x] 標準設定では旧TLSを拒否し、選択した範囲と必要な旧TLS互換設定でTLS 1.0／1.1の実転送が成功する。
- [x] TLS 1.2以下と1.3の暗号指定を独立して適用し、共通暗号なし・全て未知の暗号指定・未対応バックエンドをエラーにする。
- [x] AUTHの優先順を変更してもTLSの選択範囲を変えず、両コマンドを拒否された場合は認証しない。
- [x] 旧TLS互換でも証明書の未信頼・期限・名前不一致を既定では拒否し、他ウィンドウやシステム設定へ影響しない。
- [x] ルート全体テストが成功し、INIから起動した実ウィンドウで旧TLSのファイル操作を確認できる。

コミット例: `feat: configure FTPS TLS versions and legacy compatibility`

## 8. 段階4: 証明書検証失敗をオーバーレイで許可できるようにする

成果物: INIの `certificate_error_action=prompt` を指定した実ウィンドウで、証明書の検証失敗を確認して許可・拒否できる。

1. 既存のファイル転送オーバーレイを使い、`reject` と `prompt`、正常証明書、未信頼、期限切れ／有効期間前、名前不一致のテストを先に全体RED確認する。許可前のUSER／PASS送信がないこともfixtureで検証する。
2. 証明書情報・検証失敗・例外の対象を表すFTP内部モデルと、非同期の確認応答を追加する。`FtpClientOpenOptions` にはUIに依存しない問い合わせ口を設け、GTK処理は `file-transfer-main.cpp` から既存オーバーレイへ接続する。
3. OpenSSL公開APIを用いた証明書検証の内部コンポーネントを追加する。正常検証、失敗情報取得、承認済みの証明書・失敗だけを許容する経路を分け、CA検証・ホスト名検証のどちらも例外の対象と照合する。
4. 初回認証前は承認後の新規ハンドシェイクから再開する。データ接続や操作途中の失敗は、再実行してよいかを操作状態から判断する。既に始まった変更操作を自動で再送せず、失敗と部分ファイルの状態を通知する。承認後に失敗状態のクライアントを再作成し、同じ論理セッションで次の操作を行えるようにする。
5. 確認待ち・許可直後のキャンセル、長い回答待ち、確認中のウィンドウ終了、証明書変更、別ウィンドウ、新規接続を検証する。許可状態を表示し、READMEへ例外の範囲と失効条件を記載する。

完了条件:

- [x] `reject` では許可UIがなく、`prompt` では検証失敗時だけ同じウィンドウ内のオーバーレイを表示する。正常な接続には確認が出ない。
- [x] 接続先・失敗理由・証明書情報を表示し、許可で対象の接続が進み、中止・Esc・ウィンドウ終了では認証・転送を続行しない。
- [x] 許可前に対象の制御接続へUSER／PASSを送らず、未承認のデータ接続でファイル内容を送受信済みとして採用しない。
- [x] 同一セッション内の同じ証明書・失敗では重複確認せず、証明書・失敗の変更、別接続、手動再接続では改めて確認する。例外は永続化しない。
- [x] データ接続で異なる証明書を提示する相手も確認し、承認のための再試行でSTOR・削除・名前変更等を二重実行しない。
- [x] 旧TLS互換と `prompt` の組合せが動作し、証明書の許可でTLSバージョンや暗号を勝手に緩めない。
- [x] 証明書確認中もUIが応答し、閉じた後の遅延応答・worker・待機処理を残さない。動画による確認を含むルート全体テストが成功する。

コミット例: `feat: confirm FTPS certificate failures in an overlay`

## 9. 段階5: ランチャーの設定UIからFTPSを使えるようにする

成果物: 利用者がINIを編集せず、両FTPS方式、TLS詳細、CA、証明書エラー方針を選び、接続を開始できる。

1. 共有設定UIとランチャーで、暗号化方式、TLS詳細、CA選択、証明書エラー方針、ポート表示、保存・適用・取消・継承を操作するテストを追加してRED確認する。
2. `settings-widget.cpp`、`settings-presentation.cpp`、対応するテストfixtureと翻訳へ設定項目を追加する。実効ポートの計算を重複実装しない。
3. 英語・日本語のFTPS表示と失敗理由を確認する。証明書エラーとログイン拒否を混同せず、必要な修正内容を既存エラーパネルで確認できるようにする。
4. ランチャーで接続を作成・保存し、起動した実ウィンドウから両FTPS方式、旧TLS、証明書確認を経たファイル転送を完了するシナリオを追加する。READMEの利用手順もUIからの操作へ更新する。

完了条件:

- [x] 接続設定とグローバル設定で暗号化方式とCAを選べる。独自CAの継承からシステムCAへの明示的な切替も保存・復元できる。
- [x] TLSの範囲・固定、AUTH優先順、暗号リスト、旧TLS互換、証明書エラー方針を選べる。旧TLS互換の選択と証明書例外の許可を混同しない。
- [x] 方式変更時のポート表示、明示ポートの保持、リセット、保存、取消、外部再読込みが正しい。
- [x] 不正なTLS設定をUIで確認・修正でき、修正前には接続しない。独自CAファイルの未入力などで保存・適用を止める。
- [x] 実行中はTLS詳細・CA・証明書エラー方針を編集できず、次回起動した接続に保存した設定を適用する。`prompt` に基づくオーバーレイへの回答は可能。
- [x] ランチャーからの実FTPS転送、英語・日本語の表示とエラー通知、必要な画像の目視確認を終え、ルート全体テストが成功する。

コミット例: `feat: expose FTPS TLS and certificate policy settings`

## 10. 段階6: 配布環境と実サーバーで検証する

成果物: 開発環境と配布パッケージの両方でFTPSを利用でき、対応範囲が利用者向け文書に反映されている。

1. 配布パッケージの実行時依存へ `ca-certificates` を追加し、OpenSSL版libcurlと証明書コンポーネントの直接依存が整合することを確認する。段階1・4で追加した依存が全対象の `prereq.sh` 経路と生成パッケージに反映され、インストール後に動作することをテストする。
2. 一時的な隔離コンテナの信頼ストアへテストCAを登録し、`ca_file` 未指定で成功すること、未登録では拒否されることを確認する。ホストの信頼ストアは変更しない。
3. 既存の `ELDER_TERMS_TEST_FTP_APP` による実行対象切替を使い、インストール済みアプリでも全ウィンドウシナリオを含むルート全体テストを実行する。必要な共有ライブラリとTLSバックエンドが配布先で解決されることを確認する。
4. 最低対応のlibcurl 7.88.1を使うDebian 12の検証環境と、通常の64bit環境、既存のDebian trixie i386経路でビルド・全体テストを確認する。最低版環境の選定根拠は[Debian bookwormのlibcurl4パッケージ](https://packages.debian.org/bookworm/libcurl4)。i386では `ELDER_TERMS_TEST_DEBIAN_TRIXIE_I386=1` を付けて既存の任意検証を有効にする。
5. fixture以外の実FTPSサーバー（vsftpdを候補）を隔離コンテナで起動し、明示的／暗黙的FTPSとTLSセッション再利用要求のあるデータ転送を確認する。[vsftpd公式設定文書](https://security.appspot.com/vsftpd/vsftpd_conf.html)と採用版の同梱man pageを確認し、再現可能な設定をテスト側に置く。
6. 配布対象ごとにTLS 1.0／1.1の明示許可、バージョン・暗号固定、証明書の拒否／確認／許可を検証する。バックエンドやシステムポリシーで未対応の項目を無視しないことも確認する。
7. README英語・日本語へ方式・TLS詳細の選択、旧プロトコルの対応限界、証明書と接続先名、独自CA、検証失敗時の設定と例外の有効範囲、libcurl・OpenSSL・CAパッケージの必要条件を記載する。FTPSでは暗号化された制御内容をNAT機器が読めないため、データ接続用の経路設定が必要になる点には[公式FTPS解説](https://everything.curl.dev/ftp/ftps.html)を付ける。

完了条件:

- [ ] 開発ツリーとインストール済みパッケージの両方で、両FTPS方式の実ウィンドウ転送が成功する。
- [ ] システムCAと指定CAの両経路で成功・拒否を検証し、パッケージにCAの実行時依存を含める。
- [ ] 最低libcurl版、通常64bit、i386で必要なビルド・全体テストが成功する。
- [ ] 配布対象ごとに旧TLS互換と証明書確認が動作し、libcurlと証明書コンポーネントのOpenSSLの整合を確認する。
- [ ] fixture以外のサーバーでも保護されたデータ転送とセッション再利用を確認する。検証したサーバー・libcurl・TLSバックエンドの版を記録する。
- [ ] README、翻訳、公開コメントに実装済み機能との矛盾がなく、外部依存による制約には出典がある。

コミット例: `chore: validate and package FTPS support`

## 11. 最終完了条件と実施記録

実装完了時は、各段階の完了条件と成果物を照合し、以下を全て確認する。

- [ ] 両FTPS方式を設定UIから選択し、既存のファイル操作を暗号化して実行できる。
- [ ] TLS・設定の異常では接続を止める。証明書異常は `reject` では拒否し、`prompt` では利用者が許可した対象に限って進める。平文FTPへ暗黙に切り替わらない。
- [ ] バージョン範囲、暗号、AUTH優先順、旧TLS互換を選択でき、明示許可した旧TLSで実転送できる。未対応のSSLv2／SSLv3を対応済みと表示しない。
- [ ] 証明書の許可前の認証・データ送信、例外の他接続への流用、暗黙の恒久保存、変更操作の二重実行がない。
- [ ] 設定の継承、方式別ポート、独自CA、実行中の設定保護とエラー表示が整合する。
- [ ] 平文FTP、SFTP、その他の既存機能を含むルート全体テストが成功し、TLS中のキャンセル・終了と配布環境の検証も完了する。
- [ ] 各段階が完了条件を満たした状態でコミットされ、不要な暫定コード・テスト専用秘密鍵を成果物へ残さない。

計画作成時の確認: 現行コードと直前のlibcurl移行履歴、公式文書、導入済みAPIコメント、設定の継承・不正値処理、実FTPテストと配布経路を調査した。追加指定に合わせて6段階へ更新し、各段階の観測可能な成果物、全体RED／GREEN、段階別コミット、最終完了条件を記載した。リポジトリの変更は計画文書のみで、ビルド手順への影響がないためプロジェクトのビルド・全体テストは実行していない。

### 追加調査の実験記録（2026-09-10）

libcurl 8.5.0／OpenSSL 3.0.13を使用するシステムのcurlと、一時的なローカルFTPSサーバー（Node.js 24.11.1、内蔵OpenSSL 3.5.4）で検討用の接続実験を行った。サーバーはループバックだけで待受け、実験用証明書は都度生成し、鍵と証明書は終了後に削除した。証明書検証を無効にするオプションは使用していない。

| 実験 | 観測結果 |
| --- | --- |
| TLS 1.0／1.1を最小・最大に指定し、暗号条件は通常 | 明示的・暗黙的ともTLS交渉を拒否（curlエラー35） |
| 同じバージョン指定に `DEFAULT:!aNULL:!eNULL:@SECLEVEL=0` を追加 | 両FTPS方式で認証とLIST成功。制御・データ両方のTLS 1.0／1.1をサーバーで確認 |
| TLS 1.2／1.3の固定 | 両FTPS方式で制御・データのTLSとLIST成功 |
| 最小1.2でTLS 1.0のみの相手へ接続 | バージョン不一致で拒否 |
| TLS 1.2で `AES128-SHA`、TLS 1.3で `TLS_AES_128_GCM_SHA256` を指定 | それぞれ指定した暗号でLIST成功 |
| 共通暗号がない／暗号リストが全て未知 | それぞれcurlエラー35／59で拒否 |
| 旧TLS互換を使い、証明書が名前不一致／未信頼 | curlエラー60で拒否。USER送信なし |

19シナリオのうち10件が一覧取得成功、9件が想定した拒否となり、成功時の暗号化された認証・データ接続と、拒否時の認証未実行を確認した。実験スクリプトと結果は `/tmp/elder-terms-ftps-legacy.lVR1wi/probe.mjs`、`results.json`、`probe.log` に保存した。一時ファイルの保存先は恒久的な検証資産ではないため、再現条件と結果はこの文書にも記録している。

これは採用するlibcurl設定の成立性の確認であり、elder-terms本体、実FTPSサーバー製品、Active、アップロード／ダウンロード、証明書例外オーバーレイの実装検証ではない。それらは上記の各段階で実施する。特に証明書例外は設計とAPI調査までで、動作確認済みとは扱わない。

実装開始後は、各段階についてREDの失敗内容、GREENの全体実行結果、動画・画像の確認、配布環境、コミットID、必要に応じた計画の修正理由をここへ追記する。

### 段階1の実施記録（完了）

- 全体RED: 共有UI103件成功、ランチャー87件成功・1件失敗・1件任意スキップ、C++115件成功・2件失敗。不正TLS値の保持とFTPS接続で期待した不足を再現した。新しいテスト定義が `build_tests=false` にも入った配布テストの失敗は、既存のテスト有効条件内へ定義を移動して修正した。
- 実装後の全体実行ではC++116件が成功し、FTPSの連続操作で失敗した。調査したfixtureは一つの制御接続が終了するまで次の接続を受け付けず、接続を張り直すクライアントが挨拶を受信できなかった。計画のfixture拡張を補足し、TLS fixtureでは同時に存在する制御接続を受け付ける構成へ変更する。アプリ側の追加workerや自動的なTLS条件の緩和は行わない。
- 以後の全体実行は `npm run test --workspace=elder-terms-vte --workspace=elder-terms-shared --workspace=elder-terms` で全3ワークスペースを対象とし、C++側を先に観測する。実行対象を絞る指定は使用しない。
- 修正後のC++全体検証118件が成功。FTPS専用の9ケース（実ファイル往復、名前不一致、期限切れ、未信頼、CA読込み失敗、AUTH拒否、PROT拒否、データTLS失敗、不正方式）も全て成功した。TLS機能のないlibcurlではFTPSのみ拒否するケースと、不正設定の保存・再読込みも確認した。
- 実ウィンドウの明示的FTPSシナリオで、送受信、日本語名への変更、削除、ディレクトリ移動を確認。`test-results/20260910_233744_876/uses-the-real-FTP-service-through-its-window-ftps-explicit/captures/ftp-before-window-close.png` を目視し、FTPSタイトルと転送後の2ペイン表示を確認した。後続の全体実行でも成功した。

- 続く全体実行ではC++118件とFTPS実ウィンドウ操作が成功したが、既存のフォント描画テストがIBM Plex Sans JP未導入で失敗した。テスト要件を満たすには同フォントの導入が必要。検証用に[IBM公式配布物](https://github.com/IBM/plex/tree/master/packages/plex-sans-jp)を一時ディレクトリへ配置し、システム設定を含む独立したFontconfig設定で認識を確認した。次の全体実行には `FONTCONFIG_FILE=/tmp/elder-ftps-work/fonts.conf` を指定する。製品のフォント処理とホストのフォントストアは変更しない。

- 全体GREEN: C++118件、VTE 362件、共有UI103件、ランチャー88件成功。任意のi386配布テスト1件はこのホスト実行ではスキップし、段階6で扱う。ログは `/tmp/elder-ftps-work/stage1-green-3.log`。段階1の全完了条件と照合し、明示的FTPSの暗号化、既定拒否、設定保持、実ウィンドウ操作、既存機能の回帰検証を確認した。

### 段階2の実施記録（完了）

- 段階1は `9a8ab22` でコミットした。
- 全体RED: C++116件成功・3件失敗、共有UI103件成功、ランチャー88件成功・任意1件スキップ。暗黙的FTPS未実装、既定ポート990、FTPS能力検査の不足を再現した。一覧形式の追加テストで誤ったfixture引数も検出し、既存の `--unix`／`--dos` へ訂正した。
- 明示的FTPSのActive／Passive×IPv4／IPv6、TLS 1.2／1.3のセッション再利用、24種類のストリーム検証、3種類のクライアント終了検証はRED実行時にも成功した。
- GREENでは既存のVTEテスト設定 `ELDER_TERMS_VTE_TEST_FILE_PARALLELISM=true` とVitestの `--maxWorkers=2` を使用してファイル間を並行実行する。3ワークスペースの全テストを引き続き対象とし、テスト件数や検証内容は削減しない。

- 実装後のC++119件が成功。FTPS runner内の87ケースも成功した。暗黙的FTPSの実ウィンドウ転送画像と、最終応答待ちのFTPS転送をキャンセルする動画から抽出した前後フレームを目視確認した。成果物は `test-results/20260911_085342_089/uses-the-real-FTP-service-through-its-window-ftps-implicit/` と同runの `ftps-cancel-final/` 配下。転送中オーバーレイが消え、中断したファイルが確定されず、終了できることを確認した。全体GREENはC++119件、VTE366件、共有UI103件、ランチャー88件成功・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage2-green.log`。段階2の全完了条件を満たすことを確認した。

### 段階3の実施記録（完了）

- 段階2は `b86d0e7` でコミットした。
- TLS固定・範囲、暗号、AUTH、旧TLS互換、設定継承・不正値、非OpenSSLバックエンドのテストを先に追加する。
- この段階から全体実行に `--fileParallelism --maxWorkers=4` を指定する。各テストは隔離環境を使い、全3ワークスペースの検証内容は維持する。Vitestの公式CLI文書と導入済み型コメントで指定方法を確認した。

- 全体RED: C++117件成功・3件失敗、共有UI103件成功、ランチャー88件成功・任意1件スキップ。新しいTLS設定の未実装と継承範囲の検証不足を確認した。非OpenSSLケースも失敗したが、後続の調査でテスト入力のユーザー名欠落と判明したため、その失敗をバックエンド拒否のREDとは扱わない。ログは `/tmp/elder-ftps-work/stage3-red.log`。
- libcurlの `CURLOPT_SSLVERSION`、`CURLOPT_FTPSSLAUTH`、`CURLOPT_SSL_CIPHER_LIST`、`CURLOPT_TLS13_CIPHERS` の公式文書と導入済みcurlヘッダーのコメントを確認し、範囲のOR指定と旧ヘッダー向けlongキャストを適用した。

- 最初の実装後検証ではFTPS専用ケースが全て成功し、C++全体では非OpenSSLテストだけ失敗した。INIから設定を再構築する追加テストがユーザー名を失っており、`Invalid FTP username` で能力検査より前に終了していた。テストに有効なユーザー名を設定して再検証する。バックエンド検査の製品コードは変更しない。
- 最初の実装後全体実行が完了: C++119件成功・上記テスト入力による1件失敗、FTPS runner内151ケース成功、共有UI103件成功、ランチャー88件成功・任意1件スキップ。C++失敗によりVTEのUIテストはこの実行では開始されなかったため、入力訂正後に全3ワークスペースを再実行する。
- 訂正後の全体実行でC++120件とFTPS runner内151ケースが成功した。TLS 1.0の実ウィンドウ操作も完了し、`test-results/20260911_095208_315/uses-the-real-FTP-service-through-its-window-ftps-legacy/captures/ftp-before-window-close.png` を目視した。FTPSタイトル、転送したファイル、移動先ディレクトリの表示を確認した。VTE全体・共有UI・ランチャーの最終結果は待機中。

- 全体GREEN: C++120件、VTE367件、共有UI103件、ランチャー88件成功・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage3-green-2.log`。段階3の全完了条件と照合し、TLS範囲・暗号・AUTH・旧TLSの実転送、設定検証、セッション間の独立性、実ウィンドウ操作と既存機能の回帰検証を確認した。

### 段階4の実施記録（完了）

- 段階3は `8a02a7b` でコミットした。
- 証明書確認の実ウィンドウシナリオ（許可・中止・Esc・Enter・終了、両方式、旧TLS、正常証明書、未信頼・期限・名前不一致、データ接続固有の証明書）と、非OpenSSL／バージョン不一致の能力検査を先に追加して全体REDを確認する。
- データ接続固有の証明書fixtureでは別のSSL_CTXを使い、制御接続のTLSセッション再利用が異なる証明書の提示を隠さないようにする。承認後の失敗した一覧操作は利用者のRefreshで再試行し、転送・変更操作を自動再実行しない。
- 全体RED: C++120件成功・2件失敗、共有UI103件成功、ランチャー88件成功・任意1件スキップ。失敗は `certificate_error_action: non-default values are not implemented yet` であり、確認モードの未実装を確認した。C++失敗によりVTEのUIテストはこの実行では開始されなかった。ログは `/tmp/elder-ftps-work/stage4-red.log` と `stage4-red-meson.log`。
- OpenSSLの公開検証コールバックでチェーン検証を維持し、コピーした証明書情報を呼出し側へ渡す。ホスト名はOpenSSLの公開照合APIを使用し、libcurlのSAN優先・末尾ドット・ワイルドカード・最終CNの条件を専用の証明書コピー上で揃える。公式文書・導入済みAPIコメントを確認し、名前照合・例外範囲の実TLSテストを追加した。
- 証明書の許可は対象の検証エラーだけを消去し、初期認証前の制御ハンドシェイクのみ再試行する。データ接続での許可では現在の操作を失敗として返し、次の明示操作で新しい接続を使う。
- 最初の実装後全体実行: ビルド成功、C++121件成功・1件失敗、共有UI103件成功、ランチャー88件成功・任意1件スキップ。追加したIDN照合テストが国際化ドメイン名を変換できず失敗した。libcurl 8.5の[公式実装](https://github.com/curl/curl/blob/curl-8_5_0/lib/idn.c)は現在のロケールを使う `idn2_lookup_ul` を呼ぶが、単独のC++テストはロケールを初期化していなかった。実アプリと同じ `initialize_localization()` をテスト起動時に呼ぶよう修正し、製品の名前照合条件は変更しない。
- コード照合で、新しい論理接続の再確認テストが継続側のCA指定と許可応答を入力へ反映していないことも確認した。テスト入力の生成を訂正する。このケースは上記IDN失敗でまだ実行されていないため、動作確認済みとは扱わない。ログは `/tmp/elder-ftps-work/stage4-green.log` と `stage4-green-meson.log`。
- 訂正後の全体実行でC++122件、FTPS runner内188ケースが成功した。IDNを含む名前照合、証明書変更と例外範囲、承認前の認証拒否、両方式とActive／Passiveのデータ接続固有証明書、明示的なSTOR再試行を確認した。
- VTEのUIテスト開始時に、追加テストの `String.replaceAll` が既存のES2020設定で型エラーになった。改行除去をグローバル正規表現の `replace` へ訂正する。設定の対象言語版や製品コードを変更する必要はない。ログは `/tmp/elder-ftps-work/stage4-green-2.log`。共有UI103件は成功し、ランチャーの結果は待機中。
- 上記の全体実行が完了し、ランチャー88件成功・任意1件スキップ。TypeScriptの改行処理を訂正して全3ワークスペースを再実行する。
- 実ウィンドウのFTP／FTPS26ケースは23件成功・3件失敗となった。失敗は新しいテスト条件によるものだった。許可後の画面では確認パネルが消えてReadyになっていたが、従来の小さな転送パネルから流用した「20%以上の画素差」条件に対し、証明書パネルの差は約17.3%だった。動画中の各状態を安定した実画面と照合し、パネル非表示・Ready・一覧取得・前後の画面変化を検証する形へ訂正する。
- 日本語ケースは `LANGUAGE=ja` とC.UTF-8ロケールだけを指定していたため、実アプリの `ui_language=ja` 設定を明示して検証する。一律拒否ケースは既存のエラー表示が内部のacceptボタンをCloseとして利用することをテストが誤認していた。表示される操作がCloseのみで、それを押すと認証せず終了することを確認する形にする。
- 追加のコード照合で、旧TLS＋確認のUIケースのサーバー・クライアント条件が旧TLS設定を適用していないことも確認した。両方に旧TLS設定を適用し、サーバーが記録した制御・データ接続のTLSバージョンをアサートする。C++での旧TLS＋確認188ケース内の検証は成功済みだが、UIケースはこの訂正後に再確認する。
- `test-results/20260911_105807_940/` の証明書確認（英語・データ固有・期限切れ・有効期間前）、許可後のReady画面、一律拒否時のClose画面を目視確認した。日本語ケースでは英語表示になっていることも確認し、上記の起動設定不足と照合した。VTEの他の全体テストは継続中。
- 上記全体実行が終了: C++122件、FTPS runner内188ケース、VTE378件成功・上記3件失敗、共有UI103件、ランチャー88件成功・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage4-green-3.log`。テスト条件を訂正し、全体を再実行する。
- 再実行のC++122件とFTPS runner内188ケースが成功。`test-results/20260911_112802_911/` の日本語確認画面と、`ftps-prompt-allow/` の動画から抽出された `ftps-certificate-frame-1.png`／`ftps-certificate-frame-2.png` を目視した。確認中のオーバーレイと操作制限、許可後の一覧と `Ready — Certificate exception active` の表示を確認した。全体実行は継続中。
- 同runの旧TLS＋確認ケースではサーバー記録の全制御・データ接続がTLS 1.0となり、転送後の終了まで成功した。一律拒否ケースの画面を目視しCloseだけが表示されることを確認。記録にはAUTH TLSのみがあり、USER／PASSは送信されていない。
- 訂正後のVTE全体381件が成功した（15ファイル、1117.65秒）。共有UIとランチャーを引き続き実行し、全ワークスペースが終了してから段階4の完了を確定する。

- 全体GREEN: C++122件、FTPS runner内188ケース、VTE381件、共有UI103件、ランチャー88件成功・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage4-green-4.log`。段階4の全完了条件と照合し、検証失敗だけの確認、許可前の認証・データ拒否、例外範囲と再確認、変更操作の自動再送防止、旧TLSとの独立性、確認中の終了と動画、既存機能の回帰検証を確認した。

### 段階5の実施記録（完了）

- 段階4は `0fe133a` でコミットした。
- 共有設定UIに暗号化方式、TLS範囲・AUTH・互換性・証明書方針、CA選択、ポート追従、保存・再読込み、取消・継承、外部再読込み、不正値の修正、実行中の読取り専用、日本語のテストを先に追加する。ランチャーにも新規接続を作成し、両方式と旧TLS＋証明書確認で実転送する4ケースを追加する。
- この段階の全体実行は、変更箇所の結果を早く観測するため、共有UI、ランチャー、VTEの順にする。全3ワークスペースの全テストを対象とし、同じビルドディレクトリのビルド・テストを重複起動しない。全体実行の終了を待ってから修正を適用する。
- 共有UIのRED: 既存103件成功、追加10件失敗。9件はTLS・CA項目がまだ存在しないことを検出し、1件は保持された不正TLS設定でもApplyが有効な不足を検出した。ログは `/tmp/elder-ftps-work/stage5-red.log`。ランチャーとVTEの全体実行は継続中。
- ランチャーのRED: 既存88件成功・任意1件スキップ、追加4件失敗。新規接続の作成・FTP選択までは進み、暗号化方式の設定項目が存在しないことを検出した。VTE側のC++・実ウィンドウを含む全体実行を引き続き待つ。
- 段階4の追補: コードとlibcurl 8.5の公式 `urlapi.c`／`vtls.c`／`openssl.c` を再照合し、確認経路が `CURLUPART_HOST` の設定だけで正規化を済ませ、末尾ドットをIP判定前に削除している不足を見つけた。実際の転送はURL全体の解析で短縮・16進IPv4を正規化するため、同じ入力の通常検証と確認経路がずれる可能性がある。計画の「libcurlの名前照合条件と揃える」を、URL全体の再解析と、IP／DNS判定後のDNS末尾ドット処理まで明示して補強する。
- 追加検証は両方式×拒否／確認×4つの数値ホスト表記・証明書条件の16ケースとする。短縮・16進IPv4は既存クライアントの実転送、末尾ドット付きの数値名はテスト専用の `CURLOPT_RESOLVE` でループバックへ固定した暗号化LISTで検証する。通常libcurlの成功／証明書エラーを参照結果にし、確認経路にも同じ結果と必要時だけの確認を要求する。参照側ではIP SANとDNS SANのちょうど一方が一致することも確認する。ホストDNS設定や外部DNSに依存しない。
- 追加テストは現在の全体RED終了後に適用し、VTE、共有UI、ランチャーの順で再び全体REDを確認する。その後にホスト名処理の根本修正と設定UIを適用し、全体GREENを確認する。ホスト名の修正・回帰テストは設定UIとは別の修正コミットとして記録する。
- 最初の全体REDが終了: 共有UI103件成功・追加10件失敗、ランチャー88件成功・追加4件失敗・任意1件スキップ、C++122件とVTE381件成功。追加UIの未実装以外の失敗はなかった。ホスト名の追加16ケースと比較用ドライバーを適用し、製品コードを変更する前に全体REDを再実行する。

- 追加の全体REDが終了: C++121件成功・FTPS runner 1件失敗、共有UI103件成功・追加10件失敗、ランチャー88件成功・追加4件失敗・任意1件スキップ。短縮・16進IPv4は両方式とも通常検証が成功し、確認経路だけが名前不一致となる4件の不足を再現した。末尾ドット比較ではテストURLがログイン先から相対homeへ移動してcurl 9となり、参照結果を得られないケースもあったため、サーバー絶対パスの二重スラッシュに訂正する。ログは `/tmp/elder-ftps-work/stage5-red-2.log`。
- URL全体の再解析とIP／DNS分類後のドット処理を修正し、設定UI・翻訳・READMEを適用する。全3ワークスペースを共有UI、ランチャー、VTEの順に再実行する。
- 最初の実装後全体実行では共有UI112件成功・追加取消テスト1件失敗。共通ウィジェットの取消はドラフトを戻して呼出し元に通知し、呼出し元が閉じる既存契約だったため、ウィジェット表示の復元ではなく取消後の保存値を確認するテストへ訂正する。英語・日本語の設定画像は `test-results/20260911_123946_657/shared/` を目視し、描画前の取得が含まれるため既存の描画安定待ちを追加する。
- 同じ実行のランチャーは既存88件成功・追加4件失敗・任意1件スキップ。追加ケースはFTPS.iniの保存待ちで止まった。大文字の名前をShiftなしの低レベルキー合成で送っていたため、既存の名前編集欄の編集インターフェイスへ文字列を設定する手順に訂正する。製品の保存処理は変更しない。
- C++全体122件とFTPS runner内204ケースが成功した。通常libcurlと確認経路を比較するホスト名追加16ケースもすべて成功。VTE実ウィンドウ全体の終了を待つ。
- 上記の全体実行が終了: C++122件とFTPS204ケース、VTE381件成功。共有UI112件成功・追加1件失敗、ランチャー88件成功・追加4件失敗・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage5-green.log`。準備したテスト条件の訂正とREADMEのUI説明整理を反映し、全3ワークスペースを再実行する。
- 訂正後の共有UI113件がすべて成功。`test-results/20260911_131054_603/shared/` の英語詳細画面と日本語詳細画面を目視し、TLS 1.3暗号の入力値、旧TLS互換と証明書方針の独立した表示、CA・暗号の継承表示とスクロールを確認した。ランチャーとVTEの全体結果を待つ。
- 再実行のランチャーは既存88件成功・追加4件失敗・任意1件スキップ。名前編集欄のAT-SPI取得ができず、入力方法の変更は成立しなかったため戻す。先の大文字キー入力という推定だけでは保存待ちの原因を確定できない。追加FTPSテストは既存の新規作成テストと同じく名前編集をEscで閉じ、既定名New connectionで保存する手順へ揃える。名前編集自体は既存テストが検証しており、このケースは新規接続のFTPS設定と実転送を検証する。Applyの有効性も保存直前に確認する。VTE全体の終了後に訂正を適用する。
- 次の全体実行から、全ワークスペースのビルドを順番に完了し、MesonのC++全体テストを実行した後、全3ワークスペースのVitestを並行実行する。検証用の単一Nodeエントリポイント `/tmp/elder-ftps-work/run-complete-suite.mjs` が全結果を集約し、各ワークスペースのファイル・テスト名は絞り込まない。既存npm testと同じビルド・全テストを実行し、ビルドとテストの競合を避ける。`npm exec --workspace` の公式文書と導入済みnpm実装コメントで実行ディレクトリを確認した。各Vitestは従来どおりmaxWorkers=4とする。
- 上記の全体実行が終了: C++122件・FTPS204ケース、共有UI113件、VTE381件成功。ランチャー88件成功・追加4件失敗・任意1件スキップ。ログは `/tmp/elder-ftps-work/stage5-green-2.log`。名前編集をEscで閉じる既存手順へ訂正し、全体を再実行する。
- 全体実行GREEN3はC++122件・FTPS204ケース、共有UI113件、VTE381件成功。ランチャー90件成功・追加2件失敗・任意1件スキップ。新規名の保存待ちは解消し、明示的FTPSと暗黙的FTPS＋旧TLS・証明書確認の実転送が成功した。`test-results/launcher/launcher-ftps-explicit-verified.png` と `launcher-ftps-implicit-legacy-prompt.png` を目視し、転送ファイル、FTPSタイトル、証明書例外の継続表示を確認した。
- 残る2件はAT-SPIのセル取得 `cellAt()` 中に対象のアクセシビリティオブジェクトが消えて失敗した。導入済みgestamentのソースマップで例外箇所を確認した。一覧更新中の読み取りを `waitForResult` 内で再取得し、マウス操作は読み取り完了後に一度だけ送る手順にする。転送開始後はReadyと両側のパスを確認してからファイルを選択し、失敗時には操作段階も表示する。製品コードは変更しない。ログは `/tmp/elder-ftps-work/stage5-green-3/`。
- 全体実行GREEN4はC++122件・FTPS204ケース、共有UI113件、ランチャー92件成功・任意1件スキップ。新規FTPSの4ケースはすべて成功し、全4画像を目視してFTPSタイトル、転送ファイル、証明書例外の表示を確認した。一方VTEは380件成功・既存のTELNET経由XMODEM送信480KB／開始遅延250msの1件が失敗した。ログは `/tmp/elder-ftps-work/stage5-green-4/`。
- 受信側rxは188ブロック付近まで受信したが、TIMEOUTによる再送、重複ブロック、Sync Errorの後に未完了ファイルを削除し、終了値128を返していた。出力ファイル待ちを延長しても完了にはならない。VTEの製品コードとこの既存テストは今回変更しておらず、FTPS変更との因果関係は未確定。検証負荷を切り分けるため、全体ハーネスを共有UI＋ランチャーの全件終了後にVTE全件を開始する構成へ戻す。全対象と各ワークスペースのmaxWorkers=4は維持し、製品・外部ライブラリ・既存転送テストの修正は行わず全体を再実行する。

- 全体GREEN5: C++122件・FTPS runner内204ケース、共有UI113件、ランチャー92件成功・任意1件スキップ、VTE381件成功（1187.31秒）。前回のXMODEMケースも内容一致まで成功したが、単一の再実行だけで失敗の原因を断定しない。ログは `/tmp/elder-ftps-work/stage5-green-5/`。段階5の全完了条件と照合し、設定UI、継承・保存・取消、CAの切替、不正値の修正、実行中の保護、日本語表示とランチャーからの実FTPS転送を確認した。
- 段階4の追補となるホスト名照合の修正と16ケースの回帰テストは `f2ac4d5` で別途コミットした。
