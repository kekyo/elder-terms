{
  "name": "elder-terms",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "meson setup ../.build .. --reconfigure || meson setup ../.build .. && meson compile -C ../.build",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "@types/node": ">=24.0.0",
    "gestament": ">=1.3.0",
    "prettier-max": ">=1.16.0",
    "typescript": ">=6.0.0",
    "vite": ">=7.0.0",
    "vitest": ">=4.0.0"
  }
}
