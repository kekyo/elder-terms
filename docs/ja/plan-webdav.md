# WebDAV対応計画

作成日: 2026-09-12。調査基準: `c65c89c`。

状態: 段階1〜6を完了。一般的なWebDAVの接続設定、共通ファイル操作、HTTPS証明書オーバーレイを実装した。同一固定ソースによる全13配布対象のビルド・インストール・全C++、開発環境と配布2環境の全UIが成功し、最終文書・配布物と全完了条件を照合した。追加メモリー診断に残る未変更の既存2件は検証記録に区別して記載した。

## 1. 目的と前提

- libcurlでHTTP/HTTPSのWebDAVに接続する。
- Generalの接続タイプにWebDAVを追加し、専用のWebDAV設定タブを設ける。
- SFTP/FTP/FTPSと同じファイル転送ウィンドウ、操作、転送進捗、競合・エラー・認証オーバーレイを使う。
- HTTPSの設定はFTPSより少なくし、無効な証明書の確認はFTPSと同じオーバーレイと許可範囲で扱う。
- 対応範囲は一般的なWebDAVのファイル操作とBasic/Digest認証・認証なしに限定する。IISやNextcloud等の特殊機能には対応しない。接続先製品の指定は実装開始の前提にせず、特定製品での動作保証は実測した範囲に限る。
- 初期対象は一覧、移動、属性表示、ダウンロード、アップロード、フォルダー作成、名前変更、削除、再帰転送、競合処理、キャンセル。WebDAVの全仕様を実装するという意味ではない。

2026-09-12の方針確認: 利用者から「一般的に想定されるWebDAVの範囲のみ対応」「IIS,Nextcloudの特殊な機能への対応は不要」と指定された。以降の設定・実装段階・検証・完了条件はこの範囲に従う。

## 2. 接続先による差と計画への影響

製品名だけで互換性は決まらない。同じ製品でも認証設定、公開パス、リバースプロキシ、権限、保存先によって条件が変わる。

| 接続先・条件 | 主な違い | 本計画での扱い |
| --- | --- | --- |
| Apache `mod_dav` / `mod_dav_fs` | WebDAVを公開する場所、認証、アクセス権を管理者が設定する。 | 独立した標準サーバーとして実接続テストに使用する。URLと認証の設定で対応する。 |
| Nextcloud | ユーザー別のWebDAV URLとアプリパスワードを使用する。独自の分割アップロードAPIもある。 | 通常のWebDAV操作を対象とし、アプリパスワードを実行時のパスワード欄で受け付ける。独自分割転送や専用ログイン連携は本計画の対象外。 |
| IISでBasic認証を許可 | 公開パス、認証、拡張子・URL・要求サイズなどのサーバー側フィルターが影響する。 | 標準操作で対応する方針。拒否された操作はHTTP応答に基づき説明する。 |
| IISでWindows統合認証のみを許可 | Negotiate/KerberosやNTLMが必要になる。 | 本計画の対象外。未対応の認証方式であることを通知する。 |
| NAS | 製品・機種・設定による差があり、「NAS」という分類だけでは認証・制限を確定できない。 | ホスト、ポート、ベースパス、CA設定は共通項目で吸収する。専用機能や機種別の動作保証は本計画に含めない。 |

根拠: [Apache mod_dav](https://httpd.apache.org/docs/2.4/mod/mod_dav.html)、[NextcloudのWebDAV接続](https://docs.nextcloud.com/server/stable/user_manual/en/files/access_webdav.html)、[Nextcloudの分割アップロード](https://docs.nextcloud.com/server/stable/developer_manual/client_apis/WebDAV/chunking.html)、[IISのBasic認証](https://learn.microsoft.com/en-us/iis/configuration/system.webserver/security/authentication/basicauthentication)、[IISのWindows認証](https://learn.microsoft.com/en-us/iis/configuration/system.webserver/security/authentication/windowsauthentication/)、[IISの要求フィルター](https://learn.microsoft.com/en-us/iis/publish/using-webdav/how-to-configure-webdav-with-request-filtering)。

接続タイプの下に「Nextcloudモード」「IISモード」などは設けない。標準の設定・操作で扱える接続先を対象とし、製品固有の追加機能は実装しない。証明書エラーの確認手順は、接続先製品によらず共通にする。

独自分割アップロード、専用ログイン連携、OAuthのブラウザログイン、Windows統合認証は本計画の対象外とする。標準WebDAVにも含まれるLOCK/UNLOCKによる編集ロック管理、PROPPATCHによる任意属性更新、サーバー内COPYについても、今回のファイル閲覧・転送で提供する操作には含めない。同期・再開転送、クライアント証明書認証、プロキシ設定も追加しない。未対応の認証方式や操作を要求された場合は理由を表示し、製品固有の処理へ自動的に拡張しない。

## 3. 現行コードから確認した変更点

| 現行コード | 確認事項と変更方針 |
| --- | --- |
| `shared/include/elder-terms/settings/general-settings.h`、`shared/src/settings/general-settings.cpp` | `ConnectionKind`と`general.type`の検証にWebDAVを追加する。 |
| `shared/src/settings/ftp-settings.cpp`、`settings-store.cpp`、`settings.cpp` | FTPの設定定義、継承元、不正値の保持を参考に`webdav-settings.h/.cpp`を追加する。WebDAVを端末プロファイル生成から除外する。 |
| `shared/src/settings-widget/settings-widget.cpp`、`settings-presentation.*` | FTPページには接続先・初期ディレクトリ・TLS設定と実行中の編集制限がある。行部品、継承、適用・取消、CA選択を再利用する。 |
| `elder-terms/src/main.cpp` | 保存済み接続と編集中接続の両起動経路で、WebDAVを`elder-terms-file-transfer`へ振り分ける。 |
| `elder-terms-vte/src/file-transfer/file-transfer-main.cpp` | FTP/SFTPの起動・認証・終了処理とFTPS証明書オーバーレイがある。WebDAVの起動を追加し、認証表示と証明書表示を必要な範囲で共通化する。 |
| `file-transfer/remote-file-client.h`、`file-transfer-window.cpp` | 一覧・転送UIは既に`RemoteFileClient`へ依存する。WebDAV用にウィンドウを複製しない。サイズは現在必須の数値であり、不明と0バイトを区別する変更が必要。 |
| `file-transfer/file-transfer-engine.cpp` | アップロードは隣接一時ファイルへ書き、最終応答後に名前変更する。書込み開始APIにサイズがない。削除APIは空ディレクトリの削除を前提とする。HTTP向けにこの境界を明示的に拡張する。 |
| `ftp/curl-ftp-session.cpp` | 現行FTPは`curl_easy_perform()`を既存workerで実行する。FTP固有オプション、応答行、制御・データ接続、終了処理が含まれ、そのままHTTPに流用できない。 |
| `ftp/ftps-certificate.h/.cpp` | セッション内の証明書例外、名前照合、OpenSSL整合確認を実装済み。FTPの制御・データ区分から独立させて共通化する。 |
| `elder-terms-vte/meson.build` | ファイル転送実行ファイルはlibcurl 7.88.1以上とOpenSSLに依存する。WebDAV実装とXML解析の依存を同じ実行ファイルへ追加する。 |

調査環境はlibcurl 8.5.0、OpenSSL 3.0.13、libxml2 2.9.14。これは導入状況の確認であり、動作検証ではない。ベンダーコードは変更しない。

## 4. WebDAV設定タブ

保存先は`[general] type=webdav`と独立した`[webdav]`セクション。FTPの設定値をWebDAVへ暗黙に流用しない。

| 表示項目 | INIキー | 組込み既定値・動作 |
| --- | --- | --- |
| 接続方式 | `scheme` | `https`。`https` / `http`を選択。 |
| サーバーアドレス | `address` | 空。ホスト名、IPv4、IPv6。接続時必須。 |
| ポート | `port` | HTTPSは443、HTTPは80。明示値を優先。 |
| WebDAVベースパス | `base_path` | `/`。サーバーのWebDAV公開先。URLのパス部分を指定。 |
| 認証方式 | `authentication` | `auto`。`auto` / `basic` / `digest` / `none`。 |
| ユーザー名 | `username` | 空。認証が必要なら接続時の入力欄で指定する。 |
| 初期ローカルディレクトリ | `local_directory` | 空。既存の転送先設定・ダウンロード先へのフォールバックを利用。 |
| 初期リモートディレクトリ | `remote_directory` | `/`。ベースパスをルートとするディレクトリ。 |
| CA証明書 | `ca_file` | 空はシステム既定。継承 / システムCA / 独自PEMファイルを選択。 |
| 証明書検証の失敗時 | `certificate_error_action` | `reject`。接続拒否 / オーバーレイで確認（`prompt`）。 |
| 接続タイムアウト（秒） | `connect_timeout_seconds` | 30。DNS・TCP・TLS確立を含む接続待機の上限。 |
| 通信停止タイムアウト（秒） | `idle_timeout_seconds` | 60。ネットワーク上で進行がない待機の上限。 |

基本項目、HTTPS項目、通信待機の順に配置する。FTPと同じスクロール・継承表示を使う。HTTPS項目はHTTP選択中は無効表示にし、入力済みの値は保持する。HTTPを選択した場合は暗号化されない接続であることを方式の表示で伝える。

- `auto`はBasic/Digestの範囲でlibcurlに選択させる。`CURLAUTH_ANY`で未計画の認証方式まで有効にしない。`none`ではユーザー名・パスワード・Authorizationを送らない。FTPの`anonymous`指定とは別の意味とする。
- パスワードはFTPと同様に接続時のオーバーレイで受け付け、そのウィンドウのメモリーにだけ保持する。Nextcloudのアプリパスワードも同じ入力欄を使う。
- HTTPSはTLS 1.2以上、最大版と暗号はライブラリの通常ポリシーとする。FTPSのAUTH順、Active/Passive、旧TLS互換、暗号一覧、TLS上下限の詳細設定は追加しない。
- ポートの解決順は接続固有値、明示されたグローバル値、schemeに応じた組込み値。方式を切り替えても明示ポートを上書きしない。
- 設定未指定は継承とする。CAの明示的な空値はシステムCAへの復帰であり、独自CAの継承とは区別する。CAファイルは絶対パスで保存し、読込み失敗は接続エラーにする。
- サーバーアドレスにscheme・パス・認証情報を混在させない。完成する接続URLを読取り専用で表示して、各欄の関係を確認できるようにする。
- `base_path`はURLエスケープを含む絶対パス、`remote_directory`は画面に表示する通常のパスとする。ベースパス末尾はコレクションとして正規化する。クエリー・フラグメント・不正なエスケープは拒否する。
- ベースパスを画面上の`/`とし、その外側へ上がる操作を禁止する。例えばベースが`/dav/`、初期リモートが`/Documents`なら、その配下から閲覧を開始する。
- ポートは1〜65535、待機秒数は1〜86400。不正値を既定値へ黙って置き換えず、FTPと同様に出所を持つ検証エラーとして保持する。適用・接続前に検出する。
- 接続中は接続方式・アドレス・認証・TLS等の接続条件を読取り専用にする。変更は保存した設定から新規接続すると反映する。人の回答待ちとローカルI/Oによる意図的な一時停止を通信停止時間に数えない。

例:

```ini
[general]
type=webdav

[webdav]
scheme=https
address=cloud.example.com
base_path=/dav/
authentication=auto
username=alice
local_directory=
remote_directory=/Documents
certificate_error_action=reject
```

認証とTLS設定の根拠: [CURLOPT_HTTPAUTH](https://curl.se/libcurl/c/CURLOPT_HTTPAUTH.html)、[CURLOPT_CAINFO](https://curl.se/libcurl/c/CURLOPT_CAINFO.html)、[CURLOPT_SSLVERSION](https://curl.se/libcurl/c/CURLOPT_SSLVERSION.html)。独自CAファイルの指定は、そのCA以外を完全に排除する機能としては定義しない。

## 5. 接続・プロトコル・共通UIの設計

### 構成と非同期処理

`webdav/webdav-client.h/.cpp`を`RemoteFileClient`の実装として追加し、内部のHTTP実行、URL処理、WebDAV応答解析を小さな部品に分ける。共通のファイル転送エンジンにはプロトコル名による分岐を増やさず、必要な能力・操作契約を渡す。

HTTP実行はlibcurl multi socket APIとcardioのFD待機・タイマーを結び付ける。アプリ側にWebDAV用workerを追加せず、UIスレッドで同期通信も行わない。現行FTP worker全体の作り直しはこの計画の対象にしない。TLSの判定部品とUIはFTP/FTPSにも実際に適用して共通化する。

- `CURLMOPT_SOCKETFUNCTION`で監視対象を更新し、cardioのFD準備完了から`curl_multi_socket_action()`を呼ぶ。FDの所有者はlibcurlであり、監視側は閉じない。
- `CURLMOPT_TIMERFUNCTION`の更新に合わせて一回限りのタイマーを交換する。0ミリ秒は次のディスパッチで処理し、コールバック内でlibcurlへ再入しない。-1はタイマーを解除する。
- 一つの論理接続内は操作を直列化し、ストリームの最終HTTP応答まで操作枠を保持する。独立ウィンドウ間で認証・接続プール・証明書例外を共有しない。
- 読書きは上限付きバッファとpause/resumeを使う。コールバック内で待機せず、例外をC ABIの外へ出さない。終了時は待機、easy handle、socket監視、タイマー、確認要求を取り消して解放する。
- 対象scheme、HTTP/HTTPS、非同期DNS、最低libcurl版を実行時検査する。最低7.88.1で使える公開APIを選ぶ。プロキシは現行FTPと同様に明示的に無効化し、環境変数で接続経路が変わらないようにする。

根拠: [multi socket API](https://curl.se/libcurl/c/curl_multi_socket_action.html)、[タイマーコールバック](https://curl.se/libcurl/c/CURLMOPT_TIMERFUNCTION.html)、[プロトコル制限](https://curl.se/libcurl/c/CURLOPT_PROTOCOLS_STR.html)。cardioは同梱の`deps/cardio/README.md`と`include/cardio.h`の`from_fd()`、キャンセル可能な`delay()`の公開説明・コメントを確認した。

### ファイル操作とHTTP応答

| 共通機能 | WebDAVでの操作 | 完了判定・補足 |
| --- | --- | --- |
| 一覧 | PROPFIND、Depth: 1 | 207本文から対象コレクション自身と直下の子を区別する。 |
| 属性 | PROPFIND、Depth: 0 | `resourcetype`、`getcontentlength`、`getlastmodified`、`getetag`等を取得する。 |
| ダウンロード | GET | 応答と受信完了を検証し、一時ローカルファイルから確定する。 |
| アップロード | PUT、その後MOVE | 同じ親の一意な一時ファイルへ送信し、最終応答後に目的名へ確定する。 |
| フォルダー作成 | MKCOL | 作成成功を確認する。既存物との衝突は共通の競合処理へ渡す。 |
| 名前変更 | MOVE | 通常の名前変更は`Overwrite: F`。転送確定での置換許可と区別する。 |
| ファイル削除 | DELETE | HTTPエラーと未確定状態を区別する。 |
| フォルダー削除 | コレクションへのDELETE | WebDAVの再帰削除として明示的に扱う。 |

仕様参照: [RFC 4918](https://www.rfc-editor.org/rfc/rfc4918.html)。HTTPメソッド名だけでは送受信の動作は切り替わらないため、[CURLOPT_CUSTOMREQUEST](https://curl.se/libcurl/c/CURLOPT_CUSTOMREQUEST.html)に加えて本文・ヘッダー・upload等を操作ごとに設定する。

- 207全体を成功と扱わず、リソース単位・プロパティ単位の結果を確認する。403を不存在と誤認しない。認証失敗、権限不足、競合、ロック、容量不足、未対応メソッドを区別して既存のエラーUIへ渡す。
- XMLはlibxml2で名前空間を解析し、接頭辞の綴りに依存しない。DTD・外部実体・XIncludeを許可せず、外部ファイルとネットワークの参照を禁止する。`XML_PARSE_NONET`だけでローカルファイル参照も防げるとは扱わない。サイズ・深さ・件数に上限を設け、超過や壊れたXMLを空の一覧として表示しない。
- `href`は絶対URIと絶対パスを正しく解決し、同じoriginとベースパス内に限定する。表示名からアクセス先を作らない。パス各要素を一度だけ復号・符号化し、日本語、空白、`%`、`#`、`+`を検証する。`..`、埋込み区切り、重複した不一致の識別子等を曖昧なまま受理しない。
- サイズ不明を0バイトに置き換えない。共通属性に不明を表せる型を設け、一覧と合計進捗も対応する。更新時刻の読取りと設定能力は分け、取得できた時刻を表示しても書き戻せるとは判定しない。
- POSIX権限、シンボリックリンク作成、時刻書込み、サーバー側ハッシュは初期対象外。SFTP専用機能のメニューは既存の能力判定で無効化する。
- OPTIONSやAllowは補助情報として使うが、PROPFIND成功の代わりにしない。読取り専用サーバーの一覧を、書込み非対応だけで拒否しない。

XML APIは[libxml2の公開parser API](https://gnome.pages.gitlab.gnome.org/libxml2/html/parser_8h.html)と導入済み`/usr/include/libxml2/libxml/parser.h`のコメントを参照した。実装時には選択する解析APIと外部参照の拒否経路を最低対応版でも確認する。

### 転送APIで必要な調整

1. `open_write_async()`に送信予定サイズを渡す契約を追加し、SFTP/FTP/fixtureも追従する。HTTP PUTでは`curl_off_t`でサイズを指定し、既知サイズのファイルを必ずchunkedで送る実装にしない。0バイト、大容量、送信中の元ファイル変更、短い読込みを区別する。
2. Basic/Digestの選択は小さなPROPFIND等で確立する。本文のある認証要求は巻戻せる小さなXMLにする。PUTで認証更新などにより巻戻しを求められ、既存の前進専用ストリームで処理できなければ明確に失敗させる。全ファイルをメモリーへ保存したり、不完全な本文を再送したりしない。
3. 一時ファイルは衝突時に上書きしない条件で作成する。通常の名前変更と、利用者が上書きを許可した転送確定を分ける。WebDAVでは目的ファイルを先にDELETEしてからMOVEする経路を避け、対応する確定操作へ置換の意思を渡す。ファイルとディレクトリが衝突する場合は個別に扱う。
4. WebDAVのコレクションDELETEを、現在の「空ディレクトリだけを削除する」APIに偽装しない。能力と明示的なツリー削除操作を追加し、エンジンの再帰削除入口から選ぶ。SFTP/FTPの子から順に削除する経路は維持する。事前に空であることを調べるだけでは、後から追加された子の削除を防げない。
5. 通信切断後のPUT/MOVE/DELETEは、HTTP上の冪等性だけを理由に自動再実行しない。最終応答前の切断は結果未確定として表示し、再読込み・利用者の再試行へつなぐ。失敗時の一時ファイル回収は自分が作成した対象だけに限定し、回収できない場合はそのパスを通知する。

根拠: [HTTPアップロード](https://curl.se/libcurl/c/CURLOPT_UPLOAD.html)、[送信サイズ](https://curl.se/libcurl/c/CURLOPT_INFILESIZE_LARGE.html)、[巻戻し要求](https://curl.se/libcurl/c/CURLOPT_SEEKFUNCTION.html)、[読込みコールバック](https://curl.se/libcurl/c/CURLOPT_READFUNCTION.html)。

### リダイレクト

無条件の自動追従は使わない。読取り操作の同一origin・ベースパス内の正規化に限り、上限5回で追従する。PROPFINDはメソッドと本文を維持できる遷移だけ扱い、ログインHTMLへの遷移を成功としない。書込み操作のリダイレクトは自動再送せず、接続先パスの修正を案内する。HTTPSからHTTP、別ホスト・別ポートへの追従と認証情報の転送は行わない。新しいlibcurlだけが持つ追従モードへ依存しない。

根拠: [CURLOPT_FOLLOWLOCATION](https://curl.se/libcurl/c/CURLOPT_FOLLOWLOCATION.html)、[HTTPヘッダーの寿命と転送範囲](https://curl.se/libcurl/c/CURLOPT_HTTPHEADER.html)。

## 6. HTTPS証明書確認のシーケンス

FTPSの`confirm_ftp_certificate_async()`、証明書失敗データ、`ftps-certificate.cpp`の検証・例外管理を共通化する。FTP固有の制御・データ区分はFTPS側で付加し、WebDAVではHTTPS接続として表示する。

1. 通常の証明書・ホスト名検証を有効にして接続する。正常なら確認なしで続行する。
2. 検証失敗ではハンドシェイクを終了し、エラー識別子と証明書情報を所有するデータへ複製する。未承認の相手へ認証情報・HTTP操作・ファイル本文を送らない。
3. `reject`ならエラー表示。`prompt`なら既存の転送画面上に接続先・ポート、理由、Subject、Issuer、有効期間、SHA-256全体を表示する。証明書由来の文字列をマークアップとして解釈しない。
4. 「この接続中は許可」「接続を中止」を提示する。中止を既定にし、Esc・閉じる・キャンセルは拒否する。直前の認証欄のEnterが許可操作に流れないようにする。
5. 許可は同じウィンドウの固定された接続条件、証明書、検証失敗の組合せに限定する。証明書や失敗条件が変われば再確認する。設定ファイル・CAストアには保存しない。
6. 許可後の新しいハンドシェイクで対象を照合する。初期の読取り・接続確認は再開できるが、利用中に失敗した変更操作は許可だけで自動再送しない。例外が有効なことを既存ステータスバーへ表示する。
7. 新規ウィンドウ・再接続・終了で例外を破棄する。待機中のキャンセルは全要求を解放し、遅れて到着した応答を閉じた画面へ適用しない。

自己署名・未信頼CA、期限切れ・有効期間前、ホスト名不一致など、現行FTPSが確認可能とする失敗だけを扱う。TLS交渉不成立、CAファイル読込み失敗、暗号条件の違反を証明書許可で緩和しない。

現行FTPSと同じOpenSSLを利用した方式を採用し、libcurlと直接リンク先OpenSSLの整合確認を共通化する。他バックエンドでは標準検証による`reject`は利用できるが、この確認方式の`prompt`は接続前に未対応と知らせる。コールバックから借用TLSポインターを持ち出さず、接続プールとTLSセッションの再利用も例外の有効範囲を越えないよう検証する。

根拠: [libcurlのSSLコンテキストコールバック](https://curl.se/libcurl/c/CURLOPT_SSL_CTX_FUNCTION.html)、[OpenSSLの検証コールバック](https://docs.openssl.org/3.0/man3/SSL_CTX_set_verify/)、既存の[FTP/FTPS利用説明](ftp-ftps.md)。最低libcurl版との整合のため、新版の非同期TLS検証APIへは依存しない。

## 7. 段階的な実装

各段階で動作を検証するテストを先に追加し、全体実行でREDを確認してから実装し、同じ全体実行でGREENを確認する。段階の途中でもビルド可能に保つ。未完成の操作を成功として見せず、その段階で提供する操作を画面で明確にする。

### 段階1: WebDAV接続を保存し、実サーバーの一覧を表示する

- 接続種別、WebDAV設定定義、設定ページ、継承・検証、二つのランチャー起動経路を追加する。
- HTTP非同期実行、URL処理、XML解析、PROPFIND、Basic/Digest/認証なしを実装し、共通ウィンドウで閲覧する。
- 一覧属性にサイズ不明を表せる型を追加し、FTP/SFTPを含む利用箇所を更新する。未取得サイズは一覧で0バイトと表示しない。
- HTTPSはシステムCA・独自CAによる通常検証まで実装する。`prompt`は段階5で有効にし、未実装の間は黙って拒否設定に読み替えない。
- HTTP/HTTPSの実通信fixtureを追加する。テストサーバーと制御スクリプトはNode.jsを使い、製品側の解析コードから独立した応答を生成する。

完了条件: 設定の保存・再読込み・継承と起動ができ、認証付きHTTP/HTTPSの実コレクションを共通画面で開ける。証明書不正、401/403、非WebDAV応答を空の一覧と誤認しない。接続・一覧取得中も閉じる操作に応答する。

コミット: `feat: add WebDAV connections and directory browsing`

### 段階2: 共通エンジンでダウンロードする

- GETのストリーム読込み、既存の一時ローカルファイル確定、進捗・取消・失敗表示を接続する。
- ローカルの既存完成ファイルを先に削除してから一時ファイルを移動する経路を見直す。最後の移動が失敗しても元の内容が残る置換操作を利用し、一時ファイルが外部処理で失われた場合も実行時テストで検証する。通常ファイルの確定で既存フォルダーと衝突した場合は、フォルダーを先に削除せず失敗として扱い、子ファイルを保持する。ネイティブの移動が使えない場合も、コピーと削除へのフォールバックによる不完全な置換を行わない。
- 段階1の共通属性を使って、不明サイズを含む合計進捗を実装する。
- 0バイト、大容量、途中切断、遅い受信先、読込み停止、サイズ不明、特殊文字のパスを検証する。

完了条件: 共通UIから単体・再帰ダウンロードでき、保存内容が送信元と一致する。途中失敗を成功表示せず、既存の完成ファイルを壊さない。不明サイズを0バイトと表示しない。全体テストがGREEN。

コミット: `feat: download WebDAV files through the shared transfer engine`

### 段階3: アップロードとファイル管理を完成させる

- 送信サイズ、転送確定時の置換指定、明示的なツリー削除の契約を追加し、既存backendを追従させる。
- PUT/MOVE/MKCOL/DELETEを共通の転送、名前変更、削除、競合オーバーレイへ接続する。
- 現行の共通画面には単独のフォルダー作成操作がないため、共通の右クリックメニューへ「新しいフォルダー」を追加する。名前の入力・取消・エラーは既存オーバーレイを利用し、ローカル側とSFTP/FTP/FTPS/WebDAVのリモート側で同じ操作を使う。空の一覧でもメニューを開けるようにし、選択対象が必要な転送・名前変更・削除は無効にする。既存項目を上書きせず、作成結果を再読込みして表示する。
- 認証巻戻し、最終応答待機、上書き競合、部分失敗、一時ファイル回収、結果未確定を実サーバーの内容で検証する。

完了条件: 共通画面から単体・再帰送受信、作成・名前変更・削除ができる。許可していない目的物を上書きせず、転送確定前に既存ファイルを消さない。部分的な207や接続断を検出し、変更操作の自動再実行が起きない。全体テストがGREEN。

コミット: `feat: support WebDAV uploads and file management`

### 段階4: FTPSの証明書処理と表示を共通化する

- 証明書の失敗データ・限定的な許可・名前照合・OpenSSL整合検査をプロトコル非依存の内部部品へ移す。
- 確認オーバーレイの表示関数を共通化し、FTPSから実際に利用する。制御・データ接続、既存設定、通常の拒否動作は維持する。
- 既存コメントを更新し、置換によって未使用になったヘルパーを削除する。旧実装との互換ラッパーは残さない。

完了条件: FTPSの正常証明書、各種検証失敗、許可範囲、データ接続、キャンセル、終了、二重実行防止を含む既存全体テストがGREEN。既存FTPSウィンドウで同じ確認操作を観測できる。

コミット: `refactor: share TLS certificate verification and confirmation`

### 段階5: WebDAVの証明書オーバーレイを有効にする

- 共通証明書部品をHTTPSの初回接続と後続接続へ適用し、WebDAVタブの`prompt`を利用可能にする。
- 認証と証明書確認のフォーカス、実行中設定、ステータス表示を仕上げる。
- 許可前のHTTP要求未送信、同一例外の再利用、別証明書・別失敗の再確認、複数ウィンドウの分離を検証する。

完了条件: 本文書6章の全手順が実HTTPS接続で成立する。拒否・Esc・Enter・閉じる・取消を確認し、許可してもTLS条件と操作の再試行方針が変わらない。FTPSを含む全体テストがGREEN。

コミット: `feat: confirm WebDAV certificate failures in the shared overlay`

### 段階6: 相互運用・配布・利用説明を完了する

- 必須の相互運用検証は自前fixtureと独立したApache mod_davで行い、一覧、双方向転送、名前変更、削除、独自CAを確認する。実測したサーバー版と認証条件を記録する。
- Nextcloud、IIS、NASの検証環境を利用できる場合は、同じ標準操作による追加確認に使う。これらの環境の用意や特殊機能への対応を完了の前提にしない。環境がない製品を検証済みとはしない。
- libxml2のビルド・実行時依存を配布定義へ反映する。最低libcurl版、既存の対応CPU・配布対象でビルドと全C++を検証し、主要配布環境では全UIも実行する。
- README両言語には利用者向けの接続方法を追加し、WebDAV詳細説明を`docs/ja/webdav.md`、`docs/en/webdav.md`へまとめる。翻訳資源・公開APIコメントも更新する。

完了条件: 開発版と配布版で基本操作と証明書確認を利用でき、自前fixtureとApache mod_davによる必須検証が成功する。実測した対応範囲・制約・出典が文書と一致する。最終完了条件を全て照合し、検証記録を保存する。

コミット: `feat: complete WebDAV interoperability and distribution support`

## 8. 検証方法と作業ルール

- 製品コードはホストgcc/g++、C++20で実装する。Makefileは既存のnpm/Mesonへ委譲する薄い入口とし、依存定義と配布経路を二重管理しない。段階1で入口を用意し、ビルドシステム全体の移行をこの機能へ混在させない。
- テストの全体実行はルートの`npm test`を基本とし、組み込まれたMesonのC++テストと全Vitestワークスペースを実行する。個別テストだけを実行して段階完了としない。
- GTK実画面は現行のgestament/Vitest方式を使う。ファイル内容、サーバーで観測した要求、ユーザー操作の結果を検証し、ソース文字列やXMLアセットの一致だけで機能を検証しない。
- 応答保留・本文送信・接続終了はfixtureの明示イベントで同期する。固定sleepによる成功判定を避ける。証明書確認と進捗表示の時間軸を検証するケースは動画を残して期待する順序を判定する。画像マスターは追加・更新時に目視確認する。
- 接続終了、pause中の取消、FD番号の再利用、タイマー交換、コールバック例外、最終応答前の切断、異なるウィンドウの同時利用を含める。
- XMLの名前空間・個別失敗、絶対href、エスケープ、非直下リソース、外部実体、重複・不正情報、応答上限、認証・リダイレクト時の本文再送を検証する。
- 証明書用の秘密鍵とCAはテスト実行時に生成し、ホストの信頼ストアを変更しない。システムCAの検証が必要な場合は使い捨て配布テスト環境内で行う。
- 依存パッケージが不足している場合は必要なパッケージのインストールを案内する。外部APIは実装時にも公式文書と導入版のAPIコメントの双方で確認し、非公開・非推奨APIを使わない。
- 計画と実装・テストに矛盾が生じた場合は、原因と計画上の仮定を先に見直す。未計画の認証方式や独自拡張を場当たり的に追加しない。

## 9. 最終完了条件

- WebDAVを新規・既存接続のタイプとして選択でき、全設定の保存・継承・検証・起動が成立する。
- FTP/SFTP/FTPSと共通の画面で、HTTP/HTTPSの一覧、双方向転送、再帰操作、作成、名前変更、削除、競合処理を利用できる。
- Basic/Digest/認証なしが指定どおりに動作し、接続先URLと認証情報の扱いに製品名の決め打ちがない。
- IISのWindows統合認証やNextcloudの独自分割転送等の特殊機能を実装せず、未対応方式は明確に通知する。特定製品の指定や特殊機能の検証を完了条件に加えない。
- HTTPSの正常検証とFTPS同等の限定的な証明書確認が動作し、許可前の送信、永続化、別接続への許可漏れ、変更操作の自動再実行がない。
- 不明サイズ、部分応答、切断、取消、終了、大容量、特殊文字、読取り専用環境を正しく扱う。
- FTP/FTPS/SFTPを含む全体回帰と配布確認が完了し、外部コードを変更していない。
- 利用者向け文書・翻訳・公開コメント・検証記録が最終実装と一致する。

計画承認時の照合: 上記の要求を設定、共通UI、プロトコル、証明書手順、各実装段階と検証へ割り当てた。一般的なWebDAVに限定する利用者の指定を反映し、特殊機能への対応や接続先製品の指定を待つ条件を除いた。計画保存時点では製品コードを変更せず、ビルド・テストは実行していない。承認後の実装結果は以下へ記録する。

## 10. 実装・検証記録

### 段階1の検証（完了）

開発ホストでは、ビルド、全C++、共有UI、ランチャーUI、VTE UIを順に実行する。各UIは全ファイルを対象とし、ファイル並列数を4、VTEのシナリオ並列数を4にする。実行中のテストが利用するビルドディレクトリでは再ビルドしない。完全な実行結果と途中失敗は `/tmp/elder-webdav-work/` に保存する。

- `stage1-red`: WebDAVを独立した接続種別として設定する実行時テストが失敗し、未実装を確認した。全C++は123成功・1失敗・任意のvsftpd検証1スキップ。共有UIとランチャーUIは成功した。VTE UIは追加テストの型エラーで起動できなかったため、これを製品のREDとは扱わず、テストの定義を修正した。
- `stage1-green2`: 全C++は126成功・任意のvsftpd検証1スキップ。実HTTP/HTTPS、Basic/Digest/認証なし、独自CA、認証・証明書拒否、リダイレクトの境界、応答待ちの取消を含む。共有UIで編集中の不正な入力がグローバル設定の再継承により消える問題を再現し、入力を保持するよう修正した。既存UIのタブ一覧の期待値へWebDAVを追加した。実ウィンドウの一覧とサイズ不明表示をキャプチャで目視確認し、応答を保留した接続のウィンドウが正常終了することを確認した。フォルダー移動テストはキー入力前のフォーカス取得を修正し、次の全体実行で確認する。
- 数値の解析失敗を保存・再読込みしても不正な入力として保持するテスト、およびXIncludeを含む応答を拒否するテストを追加した。次の全体実行でREDを確認してから製品コードを修正する。
- `stage1-validation-red`: 数値保存とXInclude拒否の2件が実行時に失敗し、残る全C++は124成功・任意のvsftpd検証1スキップ。元のINI表現と継承元の不正入力を保持するよう設定ストアを修正し、XIncludeの名前空間を明示的に拒否した。共有UIの全115件は成功し、入力保持の修正を確認した。計画の接続URL表示も未実装だったため、ランチャーの全体実行へ表示テストを追加し、表示要素が存在しないREDを確認してから、読取り専用・コピー可能なURL表示を実装した。

この実行ではランチャー起動時に`EACCES`が1件発生した。パッケージ検証の`meson install`がソース変更を検出して暗黙に再ビルドしており、実行ファイルの更新時刻と起動失敗時刻が一致した。パッケージ検証も既存の自動起動検証と同じ`--no-rebuild`を指定し、全体テスト前のビルドとテスト中のインストール検証を分離した。この実行は最終GREENには数えず、URL表示・設定ストア・XML拒否とともに次の完全実行で確認する。

`stage1-validation-red`のVTE UIは全386件が成功した。HTTP/HTTPSの認証、共通ウィンドウでの一覧、サイズ不明表示、子フォルダーへの移動と親への復帰、応答保留中の正常終了を含む。

`stage1-final-green`では、修正版のビルド、全C++の126成功・任意のvsftpd検証1スキップ、共有UIの全115成功、ランチャーUIの92成功・任意のi386配布検証1スキップを確認した。不正な数値の保存・再読込み・継承解除、XInclude拒否、接続URL表示、テスト中の暗黙の再ビルド防止を含む。

VTE UIは385成功・1失敗だった。既存SFTPの多言語行・スクロール範囲検証で、上端へ戻す入力直後の位置がまだ約1741である間に、28以下であることを検査していた。下端のキャプチャでは最終行が正常に表示されており、待機しない検査の失敗と特定した。固定時間の待機は追加せず、スクロール範囲と行の配置が更新されるまで待つ。さらに上端・下端・上端の順序をロスレス動画と保存フレームで検証する。アプリケーションのスクロール処理や外部ライブラリは変更しない。この修正後、再び全体実行で段階1の完了条件を確認する。

`stage1-scroll-green`は2026-09-12 09:37（JST）に完了。ビルド、全C++の126成功・任意のvsftpd検証1スキップ、共有UIの全115成功、ランチャーUIの92成功・任意のi386配布検証1スキップ、VTE UIの全386成功を確認した。SFTPのスクロール動画は上端・下端・上端のフレーム2・19・23を検証し、保存フレームを目視でも確認した。証拠は `test-results/20260912_091705_282/`、全体ログは `/tmp/elder-webdav-work/stage1-scroll-green/` にある。

段階1の完了条件を照合した。設定の保存・再読込み・継承、接続タイプの選択と起動、Basic/Digest/認証なしによるHTTP/HTTPSの一覧、独自CA、認証・証明書・非WebDAV応答の拒否、子フォルダーへの移動と親への復帰、応答保留中の終了が成立した。WebDAVの全12設定とURL表示は専用タブに配置され、基本操作は共通ウィンドウを使用する。ダウンロード、変更操作、証明書確認の共通化とHTTPSへの適用、配布検証は、後続段階で実施する。

### 段階2の検証（完了）

GETの実通信、40 MiB超の内容一致、空ファイル・不明サイズ・特殊文字、再帰受信、途中切断、明示取消・途中close・reader破棄後の接続再利用、意図的なpauseと通信停止時間の分離、リダイレクトと部分応答の拒否を追加した。共通エンジンには不明・桁あふれの合計サイズ、最後のローカル移動が失敗した場合の完成ファイル保持、通常ファイルと既存フォルダーの衝突を検証する。共通UIから4項目を受信し、実際のローカル内容を確認する。製品コードの修正前に `stage2-red` で全体実行する。

`stage2-red`は2026-09-12 10:10（JST）に完了。ビルドは成功。全C++は123成功・3失敗・任意のvsftpd検証1スキップで、GETの未実装と、共通エンジンの不明サイズ・合計桁あふれ・既存フォルダー保持・確定失敗時の完成ファイル保持の不足を実行時に確認した。残る1失敗は既存SSHのプロンプト順序不一致であり、今回の変更との因果関係は未確定。次の全体実行へ接続先ポート・実際の順序・期待する順序の診断を追加し、期待値は変更しない。共有UIは115成功、ランチャーUIは92成功・任意のi386配布検証1スキップ、VTE UIは382成功・WebDAV受信4失敗だった。

このREDの後、256 KiBの上限付きGETバッファー、取消・close・破棄時の要求解放、サイズ不明と桁あふれを保持する合計進捗、先行削除しないローカル確定を実装した。読込み待機中にreaderを破棄する場合と、curlのpause中に取消する場合も追加検証する。全体のGREENと段階2の完了条件の照合は、次の実行結果で行う。

`stage2-green`は2026/9/12 10:44:49（JST）に完了。ビルド、全C++の126成功・任意のvsftpd検証1スキップ、共有UIの全115成功、ランチャーUIの92成功・任意のi386配布検証1スキップ、VTE UIの全386成功を確認した。前回のSSHプロンプト順序不一致は再現せず、診断の追加以外にSSH処理や期待値は変更していない。全体ログは `/tmp/elder-webdav-work/stage2-green/`、UIの証拠は `test-results/20260912_102446_389/` にある。

段階2の完了条件を照合した。共通UIから単体を含む4項目の再帰受信が成立し、HTTP/HTTPSと各認証方式で内容が一致した。40 MiB超、空ファイル、不明サイズ、特殊文字、途中切断、確定失敗、既存フォルダーとの衝突を確認した。合計の不明・桁あふれを保持し、失敗時には完成済みファイルを残す。pause中の取消と待機中のreader破棄を含め、要求解放後の同一接続の再利用が成立した。

### 段階3の検証（完了）

PUT/MOVE/MKCOL/DELETEを処理する独立HTTP fixtureと、共通エンジン・共通UIからの実操作テストを追加した。大容量・空ファイル・特殊文字・再帰送信、通常の名前変更と許可済み上書き、ファイルとフォルダーの衝突、明示的なツリー削除、部分的な207、最終応答の切断、条件付きPUTの衝突、回収不能な一時パス、送信元の伸長・短縮、短い本文と大きい本文のDigest再認証、未対応認証の通知を検証する。サーバーが本文を読み進めない場合の書込み取消と待機中のwriter破棄、最終PUT応答待ちの取消も検証する。fixtureが実際に観測した要求数・本文・上書きヘッダーにより、自動再送や先行削除を検出する。

UIは空の一覧からの新しいフォルダー作成、取消、既存名との衝突、再帰送信、上書き確認、日本語を含むフォルダー名への変更、削除、ローカル側のフォルダー作成を対象とする。`stage3-red`で全体実行してから、送信API・変更操作・共通UIを実装する。

`stage3-red`は2026-09-12 11:17（JST）に完了。ビルドは成功。全C++は124成功・期待した2失敗・任意のvsftpd検証1スキップで、変更操作の未実装、未対応認証の通知不足、送信元サイズ変更の検出不足を確認した。共有UIは115成功、ランチャーUIは92成功・任意のi386配布検証1スキップ。VTE UIは382成功・WebDAVの4失敗で、いずれも`file_transfer_remote_new_directory_item`が存在しないためだった。既存SSHを含む他の回帰失敗はなかった。全体ログは `/tmp/elder-webdav-work/stage3-red/`、UIの証拠は `test-results/20260912_105706_940/` にある。

全体RED終了後、既知サイズのPUT、条件付きMOVE、MKCOL、明示的なコレクションDELETE、所有を確認してからの一時ファイル回収、共通のフォルダー作成操作を適用した。書込み待機中のwriter破棄と、取消時にも未確定の一時パスを実画面で通知する検証を含める。以下の操作契約に基づき、次の全体実行でGREENと完了条件を確認する。

### 段階3で具体化した操作契約

- `open_write_async(path, expected_size, permissions, cancellation)`は発見時の正確な送信サイズを受け取る。SFTP/FTP/fixtureと直接呼び出すテストも追従する。共通エンジンは読み出した量を検査し、元ファイルの伸長・短縮を転送確定前に失敗として扱う。
- `RemoteFileCapabilities.upload_commit`を持つサービスは、通常の名前変更とは別の`commit_upload_async()`で上書きの意思を受け取る。WebDAVの確定はMOVEで行い、完成済みの目的ファイルを先にDELETEしない。通常の名前変更は`Overwrite: F`、明示的な置換は`Overwrite: T`とする。ファイルとフォルダーの種類が衝突する送信は、既存内容を保持して失敗する。同種のフォルダーは従来どおり配下を転送する。
- Writerの`creation_state()`は`unknown`・`not_created`・`created`を区別する。条件付きPUTの最終201応答で作成を確認し、412は他者のファイルとの衝突として扱う。最終応答を失った場合は所有を推測しない。作成を確認できた一時ファイルだけを回収し、所有が不明な場合や回収できなかった場合はそのパスを通知する。`FileTransferCleanupFailure`により、利用者の取消時もパスの通知が通常の取消表示へ埋もれないようにする。SFTP/FTPの既存の確定・回収経路は、この能力を持たないサービスの経路として維持する。
- `recursive_directory_delete`と`remove_directory_tree_async()`を追加する。WebDAVでは選択したコレクションを一度のDELETEで処理し、207内の失敗を検査する。空かどうかの事前一覧だけでは後から追加された子を保護できないため、空ディレクトリだけを削除するAPIをWebDAVの再帰DELETEで代用しない。SFTP/FTPは子から順に削除する。
- PUTは256 KiBのバッファーから送信し、入力待ちは`CURL_READFUNC_PAUSE`で処理する。書込み・close・破棄が要求状態を所有し、取消時にはHTTP要求と監視が終了するまで保持する。本文を読み出した後の巻戻し要求と401を拒否する。小さな本文がlibcurl内部で保持される場合も、ヘッダーの401検出で再送を止める。
- 同じeasy handleのDigest認証状態を保持し、要求終了時には借用した本文・ヘッダー・コールバックデータを個別に解除する。`curl_easy_reset()`は認証状態も消すため、この境界では使わない。変更操作はHTTP/1.1の新規接続を使って再利用しない。リダイレクト、失った最終応答、消費済み本文の再認証による自動再送がないことを、fixtureが観測する実際の要求で検証する。
- 「新しいフォルダー」は両ペインの共通メニューに追加する。空の一覧でも作成でき、背景の右クリックでは古い選択を解除する。名前の検証・入力は既存の名前変更と同じオーバーレイへ集約し、作成エラーも既存の操作エラー用オーバーレイで表示する。新しいツールバーや別のブラウザウィンドウは追加しない。

APIと動作の根拠: [CURLOPT_READFUNCTION](https://curl.se/libcurl/c/CURLOPT_READFUNCTION.html)、[CURLOPT_SEEKFUNCTION](https://curl.se/libcurl/c/CURLOPT_SEEKFUNCTION.html)、[CURLOPT_HEADERFUNCTION](https://curl.se/libcurl/c/CURLOPT_HEADERFUNCTION.html)、[curl_easy_reset](https://curl.se/libcurl/c/curl_easy_reset.html)、[libcurl 7.88.1のreset実装](https://github.com/curl/curl/blob/curl-7_88_1/lib/easy.c)、[CURLOPT_FRESH_CONNECT](https://curl.se/libcurl/c/CURLOPT_FRESH_CONNECT.html)、[CURLOPT_FORBID_REUSE](https://curl.se/libcurl/c/CURLOPT_FORBID_REUSE.html)、[CURLOPT_HTTP_VERSION](https://curl.se/libcurl/c/CURLOPT_HTTP_VERSION.html)、[RFC 4918](https://www.rfc-editor.org/rfc/rfc4918.html)。公開ヘッダー`curl.h`の読込み・巻戻し・サイズ指定のコメントも確認した。

`stage3-green`は2026-09-12 11:47（JST）に完了。全C++は125成功・WebDAVクライアント1失敗・任意のvsftpd検証1スキップ、共有UIは115成功、ランチャーUIは92成功・任意のi386配布検証1スキップ、VTE UIは382成功・WebDAVの4失敗だった。他の既存機能の回帰失敗はなかった。

C++の失敗は、試験サーバーがDigestのnonceを更新した後、古いnonceに対する正しい認証応答へ`stale=true`を付けず、資格情報の誤りとして拒否していたことによる。送信本文の再利用を止めた後の独立した一覧取得が、この不整合で失敗した。[RFC 7616のstaleの定義](https://www.rfc-editor.org/rfc/rfc7616.html#section-3.3)に合わせ、正しい資格情報による期限切れと、不正なパスワードを試験サーバーで区別する。小さな本文・大きな本文の双方で、本文を消費したために再送を拒否した診断と、実際の本文送信が一度だけであることを確認する。

UIの4件は、受信後に非表示のメニューから`file_transfer_remote_new_directory_item`を取得しようとして失敗していた。既存FTPの試験と同様に、先にメニューを開いてから項目を取得する。失敗時の画面と診断も記録する。製品の認証処理やメニュー実装は変更せず、この試験修正後に全体実行を再度行う。

`stage3-green2`は2026-09-12 12:21（JST）に完了。ビルド、全C++の126成功・任意のvsftpd検証1スキップ、共有UIの全115成功、ランチャーUIの92成功・任意のi386配布検証1スキップ、VTE UIの全386成功を確認した。全体ログは `/tmp/elder-webdav-work/stage3-green2/`、UIの証拠は `test-results/20260912_120116_963/` にある。共通画面でのファイル管理と、取消後の未確定な一時パスの通知を保存画面でも目視確認した。

段階3の完了条件を照合した。共通画面からHTTP/HTTPSの単体・再帰送受信、空の一覧を含むフォルダー作成、名前変更、削除、上書き確認が成立した。既存の完成ファイルを先に削除せず、異なる種類との衝突と条件付きPUTの他者所有パスを保護する。部分的な207、最終応答の切断、送信元の伸長・短縮、取消・writer破棄を検出し、回収できたか不明な一時パスを通知する。Digest再認証で消費した本文の再送を止め、その後の独立した要求は同じセッションで処理できる。FTPS・SFTP・端末機能を含む全体回帰も成功した。

### 段階4の実装（検証中）

FTPSの証明書処理を`tls/certificate-policy.{h,cpp}`と`tls/certificate-failure.h`へ移し、確認の表示を`file-transfer-certificate-prompt.{h,cpp}`へまとめる。これは既存動作を維持する抽出であり、新しい承認条件は追加しない。段階3で成功した既存のFTPS・全体試験を抽出前の基準とし、同じ全体試験で抽出後を検証する。

- 接続先とポート、証明書情報、検証エラーを所有する値へ複製する。OpenSSLの借用ポインターを非同期の確認処理へ渡さない。
- ポリシーの識別スロットはFTPSで制御・データの2個、HTTPSで1個とする。証明書が変われば過去の許可を破棄する。FTPSの初めてのデータ接続は、現在の制御接続と同じ証明書・失敗の場合に限って既存の許可を利用できる。
- 接続先名の正規化、ホスト名検証、OpenSSLの整合確認、許可できる失敗の範囲は現行FTPSの処理を維持する。
- 共通表示はタイトルと接続ラベルを受け取る。FTPS側から既存のタイトルと制御・データ接続のラベルを渡し、既存の表示、全文フィンガープリント、文字列の無害化、既定の中止を維持する。
- 旧FTPS専用のポリシー・失敗型とファイルを削除し、全呼出し元・ビルド対象を移行する。互換ラッパーは残さない。gettext抽出対象には共通表示のソースを追加する。

基準にはメモリーBIOによる実TLSハンドシェイク、実FTPSサーバー、実ウィンドウの確認・取消が含まれる。抽出後の全体実行が終わってから完了条件を照合する。

上記の抽出を段階3のコミット後に適用した。`stage4-green`でビルド・全C++・全UIを実行し、FTPSの既存動作と共通表示を確認する。

### 段階4の全体回帰で見つかった既存SSHの境界値

共通化後の最初の全C++実行では、FTPSの実通信は成功したが、既存`ssh-channel-connection-test`のOpenSSHランダムアート比較が失敗した。通常のセルにOpenSSHは`^`を表示し、アプリケーションは`S`を表示していた。このSSH表示関数はWebDAVの実装で変更しておらず、段階4のTLS抽出からも独立している。

原因は、描画回数の上限が開始位置専用の文字`S`へ達する境界値だった。文字配列の末尾には開始・終了の2文字とNULがあり、通常セルの最大値は`sizeof(symbols) - 4`である。[OpenSSHの参照実装と説明](https://github.com/openssh/openssh-portable/blob/master/sshkey.c)では、通常セルの増加を終了マーカーの2文字手前で止めている。現在の`sizeof(symbols) - 3`は1つ大きかった。

計画の既存全体回帰を完了するため、この境界だけを先に修正する。既存の実ハンドシェイクとOpenSSH比較で今回の実行時REDを確認済みである。同じ比較を、開始・終了位置以外のセルを15回通る公開試験用鍵で確実に再現する入力へ変更し、比較の期待値や比較方法は変えない。製品コードは上限1か所と説明コメントのみ変更する。全体GREENで確認後、SSHの修正を独立した`fix:`コミットにし、TLSの抽出は予定した`refactor:`コミットへまとめる。

第4段階の初回全体検証は 2026-09-12 03:54:58 UTC に完了。ビルド、shared 115 件、launcher 92 件（任意環境 1 件 skip）、VTE 386 件は成功。C++ は 125 件成功、上記 SSH random art の既存不具合 1 件失敗、任意 vsftpd 1 件 skip。FTPS 証明書テストと既存オーバーレイの許可・拒否・キャンセル操作は成功し、画像および確認前後の動画フレームも確認した。SSH の密度上限のみを修正し、既存の OpenSSH 比較に同じ境界を必ず通る公開テスト鍵を与えて、全体を再検証する。

### 段階4の完了確認

全体再検証は 2026-09-12T04:28:19.300Z に完了し、ビルド、全C++、shared・launcher・VTEの全ワークスペースが成功した。記録: `/tmp/elder-webdav-work/stage4-green2`。C++ 126 件成功（任意 vsftpd 1 件 skip）、shared 115 件成功、launcher 92 件成功（任意 i386 1 件 skip）、VTE 386 件成功。

FTPSの正常証明書、各検証失敗と許可範囲、制御・データ接続、キャンセル・終了・二重実行防止を含む既存テストが成功し、共通オーバーレイを使用する実画面の表示と確認前後の動画も確認した。旧FTPS専用の証明書型・実装・未使用ヘルパーは残していない。段階4の完了条件を満たした。途中で検出したSSH指紋の密度上限は、既存の実SSH/OpenSSH比較によるREDと、境界入力を固定した修正後の全体GREENを確認し、独立した修正コミットにする。

### 段階5で具体化するHTTPSの確認境界

- 1つのHTTPセッションが証明書ポリシーと非同期の確認コールバックを所有する。FTPSと共通のオーバーレイへ`HTTPS certificate validation failed`と`HTTPS connection`を渡し、承認時には既存ステータスバーへ例外の有効化を表示する。
- 失敗したハンドシェイクのeasy handleとFD監視を終了してから、複製した証明書情報で確認する。HTTP要求を直列化するロックは確認中も保持し、他の要求が確認を追い越さないようにする。確認の拒否とウィンドウ終了はセッションの取消を通知する。
- libcurlが証明書検証失敗を返し、共通ポリシーが確認可能な失敗を保持する場合だけ確認する。CAファイルの読込み失敗、TLS交渉失敗などは通常の失敗として扱う。
- 許可後は証明書・失敗を再照合する新しいハンドシェイクを行う。HTTP要求送信前のGET・HEAD・PROPFIND・OPTIONSだけ再開できる。変更操作は許可後も失敗を通知し、独立した明示操作でのみ再実行する。
- `prompt`のHTTPS要求では接続再利用とTLSセッション再開を無効にし、毎回のハンドシェイクで現在の証明書を照合する。これにより、変更後の証明書へ判断した後で古い接続プールや再開セッションへ戻ることを防ぐ。通常の`reject`の接続再利用、FTPSのデータ接続の再開は維持する。確認を利用するHTTPSでは、この分だけハンドシェイク回数が増える。

この再利用制御は[CURLOPT_SSL_CTX_FUNCTION](https://curl.se/libcurl/c/CURLOPT_SSL_CTX_FUNCTION.html)の、独自のTLS条件を適用する場合の接続・TLSセッションの再利用に関する説明に基づく。[CURLOPT_SSL_SESSIONID_CACHE](https://curl.se/libcurl/c/CURLOPT_SSL_SESSIONID_CACHE.html)と導入版`curl.h`のAPIコメントも確認した。

検証には実HTTPSでの許可・中止・Esc・既定Enter・閉じる・認証Enterのフォーカス、正常証明書、未信頼CA、名前不一致、期限切れ・有効期間前、CA読込み失敗、同じ証明書の別の失敗、証明書の交換、同一プロセス内の別セッションを含める。証明書交換後の変更操作が送信されず、後続の明示操作だけが一度届くことをサーバーで検査する。実画面の確認から一覧表示への順序をロスレス動画と保存フレームで確認する。

証明書交換のfixtureは、既存のHTTP接続を閉じず、TLSチケット鍵も維持して交換する。サーバー側で再利用を強制的に防ぐと、クライアント側の古い接続・TLSセッションの再利用を検出できないためである。交換後の読取りで再確認し、変更操作の自動再送を防ぐことを、この条件で検証する。[Node.jsのTLSサーバーAPI](https://nodejs.org/api/tls.html#serversetsecurecontextoptions)と導入版の型定義コメントで、既存接続を維持する動作とチケット鍵の扱いを確認した。

段階5の実画面テストは16ケース。TLS 1.0/1.1だけを受け付ける接続先の拒否も検査し、証明書の許可で最低TLS条件を緩和しないことを確認する。新たなC++ドライバーは、実HTTPSで証明書交換後のMOVEが自動再送されないことと、同じプロセスの別セッションが例外を継承しないことを検査する。実装前の全体REDを実行する。

### 段階5の実装前検証と適用

実装前の全体検証は 2026-09-12 05:01:48 UTC に完了。ビルド、shared 115 件、launcher 92 件（任意 i386 1 件 skip）は成功。C++ は既存 126 件成功、新しい TLS セッションテスト 1 件が「WebDAV certificate confirmation is not available yet」で失敗、任意 vsftpd 1 件 skip。VTE は 390 件成功、追加した確認・正常証明書の 12 件が失敗した。確認画面を待つケースは「Failed to start WebDAV」、正常証明書のケースは「Connection failed」となり、同じ未実装エラーをアプリケーション出力で確認した。新規の拒否・CA読込み失敗・旧TLS拒否の4ケースと既存386ケースは成功。記録: `/tmp/elder-webdav-work/stage5-red/`、UI証跡: `test-results/20260912_134131_717/`。

全グループ終了後、セッション所有の検証ポリシー・非同期確認、読取りだけの再開、更新操作の再送禁止を適用した。WebDAVのアプリケーションから共通オーバーレイを呼び、FTPSと同じく所有する引数を持つpromiseを構築してからawaitする。通常の検証拒否と既存の転送処理は同じテストで再検証する。

### 段階5の完了確認

全体検証は 2026-09-12T10:24:02.265Z に完了。ビルド、C++ 127 件（任意 vsftpd 1 件 skip）、shared 115 件、launcher 92 件（任意 i386 1 件 skip）、VTE 402 件が成功した。記録: `/tmp/elder-webdav-work/stage5-green`、実画面: `test-results/20260912_190247_666/`。HTTPSの確認前後の動画フレームを目視確認した。

正常証明書は確認なしで接続し、確認可能な検証失敗は共通オーバーレイで判断できる。中止・Esc・既定Enter・終了と認証入力からのフォーカスを確認した。許可は同一接続の証明書と検証失敗に限定され、証明書交換・別の失敗・別セッションでは再確認する。確認前のHTTP送信と、交換後の変更操作の自動再送がないことを実HTTPSサーバーで確認した。CA読込み失敗やTLS 1.0/1.1の拒否は許可で緩和されず、設定ファイルも変更されない。FTPSを含む全体回帰に成功し、段階5の完了条件を満たした。

### 段階6で確認するHTTP応答の圧縮

実通信の相互運用確認には、未指定の圧縮応答を完成ファイルとして保存しない条件を追加する。libcurlの自動展開を有効にしていない場合、サーバーが圧縮した本文を渡すと、そのバイト列をファイル内容と誤認する可能性がある。まずgzipの200応答を独立fixtureから返し、既存ファイルを保持して受信が失敗するという実行時テストを全体REDへ加える。非圧縮のidentity応答とHTTPの前後空白も正常な内容として確認する。

REDで不足を確認した後、`Accept-Encoding: identity`を要求し、成功応答の非identity `Content-Encoding`を本文へ渡す前に拒否する。新しい圧縮設定や自動展開は追加せず、pause中も256 KiBの転送バッファーという契約を維持する。これは計画の「受信内容の一致・途中失敗を成功扱いしない」という条件の具体化である。

根拠: [CURLOPT_ACCEPT_ENCODING](https://curl.se/libcurl/c/CURLOPT_ACCEPT_ENCODING.html)、[RFC 9110のAccept-Encoding](https://www.rfc-editor.org/rfc/rfc9110.html#name-accept-encoding)。導入版`curl.h`の自動展開に関するコメントも確認した。

段階6のテストを追加した。9言語のWebDAV表示と日本語の設定ページを実行時に検証する。Apacheの独立検証は使い捨て配布環境で必須実行し、HTTP/HTTPS、Basic/Digest/認証なし、TLS 1.2/1.3、独自CA・システムCA・一時許可、双方向転送と変更操作を確認する。ホストでは任意環境のためスキップし、まず全体REDで翻訳とHTTP応答の不足を確認する。

### 段階6の全体REDと製品適用

開発環境の全体REDは 2026-09-12T10:57:19.053Z に終了した。ビルド成功、C++ 126 件成功・圧縮応答の1件失敗・任意環境2件skip、shared 105件成功・新しい9言語と日本語設定ページの10件失敗、launcher 92件成功・任意i386 1件skip、VTE 402件成功だった。既存機能の新たな回帰失敗はなかった。記録: `/tmp/elder-webdav-work/stage6-red`。全グループ終了後、非圧縮応答の要求と検査、9言語の表示、配布依存と同梱文書を適用した。

### 段階6の最低対応版と試験用TypeScript

開発環境の全体REDと並行し、固定したソースの別コピーからDebian 12のビルド・配布パッケージのインストール・全C++を実行した。2026-09-12 10:34:09 UTCに終了し、126件成功、WebDAV試験ランナーの2件失敗、任意vsftpd 1件skipだった。記録: `/tmp/elder-webdav-work/stage6-minimum-red/`。

libcurl 7.88.1・OpenSSL 3.0.20・libxml2 2.9.14とApache 2.4.68で、独立サーバーの39ケースは成功した。HTTP/HTTPS、Basic/Digest/認証なし、TLS 1.2/1.3、独自CA・システムCA・一時許可、40 MiB超を含む再帰送受信、空ファイル・空フォルダー・特殊文字、上書き・衝突保持・名前変更・削除を含む。

2件の失敗は、配布環境のNode.js 18.20.4が試験サーバーの`.ts`を直接読み込めず、`ERR_UNKNOWN_FILE_EXTENSION`となったことによる。C++のWebDAV処理へ到達する前の試験起動エラーであり、この2件を製品の通信REDとは扱わない。開発環境のNode.jsにあるTypeScript直接実行を、全配布CPUでも利用できるという試験側の仮定を修正する。

2つのランナーで、既存のTypeScript依存の公開`transpileModule()`を使い、共有fixtureを試験用一時ディレクトリのJavaScriptへコンパイルしてから読み込む。fixtureの動作・検証条件・製品コードは変更しない。TypeScriptの型検証は全ワークスペースを対象に別途実行する。このfixtureのimportはNode.js組込みモジュールだけであり、生成先へ相対依存ファイルをコピーする必要はない。一時ファイルは既存のfinallyで除去する。

TypeScript 6.0.3の導入版宣言と、[公式Compiler API説明](https://github.com/microsoft/TypeScript/wiki/Using-the-Compiler-API#a-simple-transform-function)、[同じ版のAPIコメント](https://github.com/microsoft/TypeScript/blob/v6.0.3/src/services/transpile.ts)を確認した。Node.js 18.20.4上のコンパイルで診断エラーがないことも確認した。全体REDの終了後に適用し、修正後の全体と最終配布マトリクスで実通信まで再検証する。

修正適用後の差分を確認した。非identityの成功応答を本文へ渡す前に拒否し、前後のHTTP空白を許可する。9言語の既存の非空翻訳が同じ内容で保持されていることを比較した。gettextの生成POTは作業記録へ移し、既存と同じくPOを管理する。利用説明の設定名・依存版と、両言語の文書の同梱・パッケージ検査を確認した。以後は同じ固定ソースで開発環境の全体GREENと配布ビルド・全C++を実行し、UI同士は重ねない。

### 段階6の全体検証で判明した配布試験とキャンセル状態の寿命

開発環境の最初のGREEN確認は 2026-09-12T11:22:00.059Z に終了した。ビルド、C++ 127件成功・任意環境2件skip、shared 115件、VTE 402件は成功した。launcherは91件成功・1件失敗・任意i386 1件skipだった。記録: `/tmp/elder-webdav-work/stage6-green`、実画面: `test-results/20260912_200707_125/`。失敗は、模擬パッケージが新しく必須にしたWebDAV文書4個を含まないことによる。模擬パッケージを更新し、各文書を1つずつ欠いた場合に実際のパッケージ検査が拒否することも確認する。製品の文書必須条件を緩めない。

配布検証のDebian 12では、x86_64の全C++が127件成功・WebDAV 1件失敗・任意vsftpd 1件skip、i386が127件成功・WebDAV 1件timeout・同1件skipだった。x86_64はHTTPSの条件付きPUTが拒否された後に異常終了し、i386はHTTPの同じ後処理で停止した。いずれもApacheの独立39ケースと既存FTPSは成功した。Debian 13 x86_64の同じソースでは全C++が成功した。最低対応版の確認は成功だけの通常転送では不足し、拒否後の通知処理まで実行する既存の故障試験が必要だった。

GDBで、両症状が解放済みのキャンセル状態を参照する同じ経路だと確認した。GCC 12のAddressSanitizerでも、`send_regular_file_async()`の結合キャンセル状態の確保・解放・再参照を記録した。式内でキャンセル通知を条件選択して直ちに`co_await`する箇所を、通知と開始したpromiseを名前付きの変数で保持してから待機する形にする。通知の意味、HTTPの再送禁止、証明書条件、cardioの実装は変更しない。関連する一時値の扱いは[GCCのコルーチン一時値の寿命に関する説明](https://gcc.gnu.org/pipermail/gcc-bugs/2022-November/805094.html)も参照したが、今回の現象をその報告と同一の不具合と断定しない。cardioの同梱READMEと公開APIコメントでも、通知・登録・取消の所有関係を再確認した。

この1か所だけを変えた別コピーで、Debian 12の全C++を実行し、2026-09-12T11:22:43Zに128件成功・任意vsftpd 1件skipを確認した。Apacheの39ケースも成功した。記録: `/tmp/elder-webdav-work/stage6-diagnostics/candidate-full.log`。全体のREDと修正候補を確認後、開発環境の全テスト終了を待ってこの修正と模擬パッケージの更新を適用した。

追加のメモリー診断では、検査ランタイムの起動時の停止を避けるため、使い捨て環境でアドレス配置を固定した全C++実行を行った。WebDAVとは別に、未変更の既存IPスキャンの一時値にも診断が出た。この追加診断を通常の全体GREENと混同せず、最終記録で結果と範囲を区別する。

未修正ソースの配布マトリクスは、開始済みのネイティブ全C++が終了した後に切り替える。未着手対象の開始を止め、テストへ進む前のARMビルド2個だけを停止した。残りを実行済みとは扱わない。修正後は別の固定アーカイブと作業ディレクトリを使用し、同じ対象・ツールチェーンのコンパイル済み出力だけを再利用してNinjaで差分を再ビルドする。配布パッケージは新規ビルドし、全13対象の全C++、独立Apache、2環境のインストール済み全UIを改めて実行する。実行中の各全体検証が読むソースは固定し、別コピーで継続する診断と混在させない。

### 段階6の全体検証の負荷と入力操作

修正後の開発環境では、`stage6-green2`が2026-09-12T11:47:10.517Zに、`stage6-green3`が2026-09-12T12:11:47.088Zに終了した。いずれもビルド、全C++、shared、launcherは成功したが、既存TELNET/XMODEMの20秒以内という時間条件が、それぞれ1件・2件失敗した。受信バイト列は一致していた。WebDAVのケースは成功した。CPUの割当てを分けるだけでは解消せず、[既存FTPSの検証計画](plan-ftps.md)と同じく、別CPU向けのコンパイルを一時停止し、ゲストの実行枠を確保して、全体UIを順番に実行する。通信の期限やテストの期待値は変更しない。

この条件で`stage6-green4`は2026-09-12T12:45:55.931Zに全体成功した。C++127件成功・任意2件skip、shared115件、launcher92件・任意1件skip、VTE402件成功。HTTPSの許可前後を記録した動画フレームも目視確認した。先行する失敗記録は保持し、成功した実行で上書きしない。

Debian 12 x86_64のインストール済み全体テストは2026-09-12T13:25:02.161Zに終了した。rootの全C++129件とApache39ケース、一般ユーザーのC++127件・任意2件skip、shared115件、launcher92件・任意1件skipは成功した。VTEは400件成功・新しいWebDAVの2件失敗だった。HTTPの認証なしとBasicのケースで、フォルダー作成後のパス入力欄のフォーカス待ちが失敗した。両方の保存画面に作成済みフォルダーと作成成功の表示があり、サーバーの記録もMKCOLとその後の一覧取得まで成功している。次の移動要求は送信されていなかった。通信失敗やフォルダー作成失敗とは扱わない。

このテストは、ネイティブ入力の前に対象ウィンドウがアクティブであるという前提を明示していなかった。[gestamentの公開説明](https://github.com/kekyo/gestament/)と導入版の`GtkWindowElement.activate()`・`GtkApp.input`のAPIコメントに従い、パス入力操作の前にウィンドウをアクティブにする。クリック後のフォーカス検査と実際の移動・転送の検査は維持する。待機時間の延長や操作の繰返しは加えない。これで解消するかは、開発環境と両方のインストール済み環境の全体実行で確認する。

この追加変更は`webdav-window.test.ts`だけで、製品・ビルド・C++試験・サーバーfixtureは変更しない。既に実行中の13対象の配布C++検証は固定ソースを維持する。UIは別の固定アーカイブで全体を再検証する。最終照合では、Markdown以外の85ファイルのうち、このUI専用ファイルだけ修正前後のハッシュを明示して区別し、その他の入力は全15環境で最終実装と一致することを必須にする。検証していない変更を既存の成功結果で代用しない。

ARMv7の最初のビルドは、既存の文字列連結に対するGCC 12の`-Werror=restrict`で停止した。検証ドライバーが既存FTPSで使用したARMv7のrelease・`werror=false`条件を引き継いでいなかったため、その環境条件を復元して再実行する。警告と最初の失敗記録は保持し、製品・ベンダーのコードを変更しない。出典は[GCCの該当警告の議論](https://gcc.gnu.org/pipermail/gcc-bugs/2022-June/790598.html)と[FTPS検証記録](../en/ftps-validation.md)。

全3ワークスペースのTypeScript型検査は、2026-09-12T13:48:27.743Zに成功した。記録: `/tmp/elder-webdav-work/stage6-final-typecheck/results.json`。既存の`npm run build`はC++ビルド、Vitestは実行時の検証であり、この型検査とは別に記録する。入力操作の修正を含む最終ソースを対象に、各ワークスペースの`tsconfig.json`全体へ`tsc --noEmit`を実行した。

修正前のDebian 13 i386の全体実行は2026-09-12T14:05:05.328Zに終了した。rootのC++129件、一般ユーザーのC++127件・任意2件skip、shared115件、launcher92件・任意1件skipは成功。VTEは401件成功・WebDAVのBasic認証1件失敗だった。同じパス入力ヘルパーのフォーカス待ちで、今回は意図的な同名フォルダー作成の拒否通知を閉じた後に失敗した。保存画面と要求記録で、フォルダーの存在と拒否処理が正しく、次のルートへの移動要求は送信されていないことを確認した。16件の証明書UIと既存FTPS・端末転送は成功し、確認前後の動画フレームも目視確認した。

両方の修正前全体記録を保持し、入力操作の修正版はDebian 12の全体実行から開始する。その後、開発環境とDebian 13 i386を順番に全体実行する。最終のUIは別の固定ソースを使用する。終了済みの同じ配布環境からコンパイル済み出力を再利用する場合も、パッケージは新規ビルド・インストールし、ビルド後の全C++と全UIを省略せず実行する。別CPU向けコンパイルの一時停止とゲスト実行枠の確保を継続し、検証中のソースを変更しない。

### 段階6の修正後UI全体検証

入力前にウィンドウをアクティブ化する前提を修正した固定ソースで、Debian 12 x86_64のインストール済み環境は2026-09-12 14:41:18 UTC、開発環境は15:13:36 UTC、Debian 13 i386のインストール済み環境は15:50:58 UTCに全体成功した。各環境で共有UI115件、ランチャー92件成功・任意1件skip、VTE UI402件が成功した。両配布環境ではrootによる全C++129件とApache39ケースが成功し、通常ユーザーによる全C++も127件成功・任意2件skipだった。開発環境の全C++も127件成功・任意2件skipだった。

証明書確認の動画フレームを3環境で目視確認した。配布2環境では実ファイル管理と、取消後に一時ファイルが残る可能性を知らせる画面も確認した。記録は `/tmp/elder-webdav-work/stage6-distribution2/isolated-repaired-results.json`、`/tmp/elder-webdav-work/stage6-green5/visual-review.json` と配布各 `installed3/*/visual-review.json` にある。全体終了後にゲスト実行枠を解放し、停止していた異なるCPU向けの4ビルドを再開した。残る全13配布対象・7実カーネルゲストの完了は、別途最終結果で照合する。

全UIの終了後、最初のDebian 12 ARM64ゲストの実行中に、空いたCPU 0〜7をゲスト用に割り当てた。配布コンパイルはCPU 8〜23を維持する。起動済みQEMUの全スレッドに割当てを適用し、以降のゲストも同じ領域を使う。全体テストの範囲・条件・タイムアウトは変更しない。変更時刻と前後の割当ては `/tmp/elder-webdav-work/stage6-distribution2/guest-cpu-allocation.json` に保存した。

未着手のUbuntu ARM64 2対象では、先行するDebianビルドの終了によって空くコンパイル枠を使い、ビルドの並列数を4から6へ変更した。コンパイルのフラグと全体テストの4並列は維持する。検証用スクリプトを別inodeへ置き換え、実行中の3コンテナーがそれぞれ元のスクリプトを引き続き参照することをハッシュで確認した。記録は `/tmp/elder-webdav-work/stage6-distribution2/ubuntu-build-parallelism.json`。

### 段階6のlibcurl 8.18で判明した取消後の再利用

Ubuntu 26.04 x86_64の全C++は2026-09-12 17:17:03 UTCに終了し、127件成功、WebDAVクライアント1件timeout、任意vsftpd 1件skipだった。libcurl 8.18.0、OpenSSL 3.5.5、libxml2 2.15.2を使用した。独立Apacheの39ケースと通常の送受信は成功したが、HTTP/HTTPS・認証なし/Basic/Digestに共通して、一時停止中の受信を取り消した後のGETが期待する内容を返さなかった。各ケースの失敗を集約する途中で300秒の全体期限となった。単に時間不足と判断せず、先に出た再利用の失敗を原因調査の対象とする。

記録は `/tmp/elder-webdav-work/stage6-distribution2/matrix/ubuntu-26.04-x86_64/` に保存する。成功済みの他対象やUIの記録も保持するが、これらを今後の修正後の検証に代用しない。全体終了後、別の固定コピーへ戻り値・HTTP状態・本文サイズ・エラーの診断だけを追加し、Ubuntu 26.04で全C++の再実行を開始した。期待値は維持する。診断実行のMeson期限は4倍とし、失敗ケースを最後まで集計できるようにする。記録は `/tmp/elder-webdav-work/stage6-curl818-diagnostics/`。

計画の取消・要求解放・同じ接続の再利用という完了条件は維持する。要求をmultiから除くだけで一時停止状態も解放できるという実装側の前提を検証する。[libcurlの一時停止API](https://curl.se/libcurl/c/curl_easy_pause.html)、[multiからの除去](https://curl.se/libcurl/c/curl_multi_remove_handle.html)、[受信コールバックの中止](https://curl.se/libcurl/c/CURLOPT_WRITEFUNCTION.html)、[送信コールバックの中止](https://curl.se/libcurl/c/CURLOPT_READFUNCTION.html)と、導入版ヘッダーのAPIコメントを確認した。[8.18.0の実装](https://github.com/curl/curl/blob/curl-8_18_0/lib/transfer.c)では一時停止を転送速度管理の状態にも保持している。内部関数は使用しない。

解除時には保存済みのデータが同期的にコールバックへ渡されるため、取消済みの受信・送信をアプリケーション側へ再配信しないことが必要になる。取消時の処理順序を診断結果に基づいて修正し、Digestの認証状態と変更操作の再送禁止も維持する。修正後は当該環境の全C++から確認し、その後に全13配布対象、開発環境と配布2環境の全体テストを改めて完了させる。

Ubuntu 26.04ではMesonのJSON記録に `elder-terms:` を付けたテスト名が現れた。Apacheの成功記録自体は存在するため、最終集計は通常の名前とこの修飾名を区別して読み取れるようにする。製品や試験の期待値は変更しない。

### libcurl 8.18での取消後再利用の修正

追加の送信取消・早期拒否試験を含む元の実装の全C++は127件成功・WebDAV 1件失敗・任意1件skipとなった。HTTP/HTTPSの11ケースで、受信の一時停止を取り消した次のGETが30秒の接続タイムアウトとなることを確認した。独立した早期403の試験は元の実装でも成功しており、このケースをREDとは扱わない。

取消時に解除する最初の修正候補では、通常操作の後続一覧が停止する1件の失敗が残った。この変更は採用しない。次の要求で接続が確立した時点に、現在のコールバックが要求した送受信の停止状態だけを適用する方式へ修正した。接続前の状態では通常のcurlイベントで待機し、ポーリングや内部APIは使用しない。Digest認証は保持し、取消済みコールバックを再配信しない。

この修正候補によるUbuntu 26.04の全C++は128件成功・失敗なし・任意1件skip、Apache39ケースも成功した。標準のテスト期限で完了し、受信・送信の取消、早期拒否、同一セッションの再利用を確認した。記録は `/tmp/elder-webdav-work/stage6-curl818-candidate2/applied-result.json`。製品と追加試験の4ファイルだけを反映し、採用しなかった取消時の処理は含めない。

修正後は全配布・全UIを新しい単一の固定ソースで再検証する。以前のUI専用ファイルのハッシュ差分は過去の記録とし、最終15コピーではその例外を設けず全変更ファイルの一致を確認する。

### 最低対応版の早期拒否の終了処理

単一の修正版ソースによるDebian 12の全体検証で、追加した送信一時停止中の早期403が完了しないことを検出した。全体メモリー診断でも同じWebDAV試験が300秒の期限に達し、125件成功・既存2件失敗・WebDAV 1件timeout・任意1件skipだった。この記録を最終GREENとは扱わない。

[libcurl 7.88.1の転送終了判定](https://github.com/curl/curl/blob/curl-7_88_1/lib/transfer.c)と[HTTP応答処理](https://github.com/curl/curl/blob/curl-7_88_1/lib/http.c)を調べた。受信が終わり送信方向が停止済みでも、送信のpauseビットだけで転送が未完了になり得る。現在のコールバックの停止状態を維持するという先の修正に加え、拒否応答の後にソケット監視が不要になった段階で、この完了済み送信方向の停止を解放する候補を用意した。認証交渉中の401はこの処理から除く。試験の期限・本文0バイト・コールバック再配信なし・同じ接続の再利用という条件は変更しない。7.88.1と8.18.0の両方で全C++を実行し、結果に基づいて反映する。

拒否後の終了処理の修正候補は、Debian 12（libcurl 7.88.1）とUbuntu 26.04（8.18.0）の両方で、全C++128件成功・失敗なし・任意1件skip、Apache39ケース成功となった。記録は /tmp/elder-webdav-work/stage6-pause-final-response-candidate/applied-result.json。この結果を確認して、製品の9行の追加だけを反映した。全3ワークスペースのTypeScriptは、先に成功した固定ソースから変更していない。以後の全配布・全UIと追加メモリー診断は、この両方の修正を含む新しい固定ソースで実行する。

最終候補の固定ソースでも追加の全C++メモリー診断を再実行した。結果は126件成功・既存2件失敗・任意1件skipで、timeoutはなくなった。WebDAV、TLSセッション、Apache、SFTP転送が成功し、残る報告はIPスキャナーとSSH TCPリゾルバーだった。両ソースファイルがFTPS基準コミットから未変更であることを確認した。記録は /tmp/elder-webdav-work/stage6-final5-asan/summary.json。通常の必須配布検証とは区別し、追加診断の全体成功とは扱わない。

固定ソースによる全13対象のビルド・パッケージ作成・インストール確認が成功した。ネイティブ6対象の全C++も成功し、実カーネルの5ゲストが全体成功した。2026-09-12 19:28 UTC時点でUbuntu ARM64の2ゲストを実行中であり、全配布ランタイムと全UIの成功は、終了後の記録で別途確認する。記録は /tmp/elder-webdav-work/stage6-distribution5/matrix/build-results.json と各ゲストのresult.json。

### 段階6の完了確認

最終確認は2026-09-12T21:36:49.030Zに完了した。全13対象のビルド・パッケージのインストール・全C++と、開発環境および2つのインストール済み環境の全ワークスペースが成功した。測定したライブラリー版、各対象の結果、Apacheの認証・操作条件、検証環境の制約は[WebDAV検証記録](webdav-validation.md)に保存した。記録は実際に検証した一般的なWebDAVの範囲に限定し、Nextcloud・IIS・NAS製品の検証を実施済みとは扱わない。

- installed-bookworm-x86_64: C++ 129件成功。elder-terms-shared: Tests  115 passed (115)、elder-terms: Tests  92 passed | 1 skipped (93)、elder-terms-vte: Tests  402 passed (402)。
- installed-trixie-i686: C++ 129件成功。elder-terms-shared: Tests  115 passed (115)、elder-terms: Tests  92 passed | 1 skipped (93)、elder-terms-vte: Tests  402 passed (402)。

入力操作の前提を修正した後の全体実行で、開発環境と両配布環境のWebDAVファイル管理・証明書確認が成功した。修正前の全体失敗と画面は保持する。libcurl 8.18の取消後再利用も修正し、全配布環境で受信・送信の取消と後続通信を確認した。最終ソースは単一の固定アーカイブを使用し、全15コピーで製品・ビルド・C++試験・fixture・UI入力の全変更ファイルと削除ファイルの不在を照合した。15個の配布パッケージへ完成した両言語の文書を反映し、文書以外のペイロード、権限、シンボリックリンクとパッケージメタデータが変わらないことを再展開して確認した。

追加のGCC 12メモリー診断は、全体126件成功・未変更のIPスキャナーとSSH TCPリゾルバーの2件失敗・任意1件skipだった。WebDAV、HTTPSセッション、独立Apache、SFTP転送の診断は成功した。通常の必須検証は成功したが、この追加診断を全体GREENとは扱わない。

記録: `/tmp/elder-webdav-work/stage6-final5-evidence/completion.json`、`/tmp/elder-webdav-work/stage6-final5-evidence/source-audit.json`、`/tmp/elder-webdav-work/stage6-final5-evidence/measured-results.json`、`/tmp/elder-webdav-work/stage6-final5-evidence/package-documents.json`。

### 最終完了条件の照合

- WebDAVの新規・既存接続タイプ、設定の保存・継承・検証・起動: 共通設定とランチャーの全体テストで確認した。
- 共通画面でのHTTP/HTTPSの一覧、双方向・再帰転送、作成・名前変更・削除・競合処理: 開発版とインストール済み配布版の実画面、独立Apacheと故障fixtureで確認した。
- Basic/Digest/認証なしとURL・認証情報の扱い: Apache39ケースおよびfixtureで確認し、特定製品の分岐は導入していない。
- IIS・Nextcloudの特殊機能を対象外とし、未対応認証方式と操作を通知する: 実装と両言語の対応範囲が一致する。
- HTTPSの通常検証と共通オーバーレイによる限定許可: 実TLS・UIで拒否、取消、既定操作、別セッション、証明書・失敗理由の変更を確認した。許可前の送信、永続化、他接続への許可漏れ、変更操作の自動再送を防ぐ。確認前後の動画フレームも目視確認した。
- 不明サイズ、部分応答、切断、取消、終了、大容量、特殊文字、読取り専用環境: 全C++と実画面の転送・取消試験で確認した。失敗時の既存内容の保持と結果未確定の通知を検証し、非同期の転送バッファーにも上限を設けている。
- FTP/FTPS/SFTPを含む全体回帰と配布確認: 最低依存版を含む全13対象と、開発環境・配布2環境の全UIが成功した。外部コードは変更していない。
- 利用者向け文書・翻訳・公開コメント・検証記録: 9言語の実行時検証、両言語の文書とパッケージ検査で最終実装との一致を確認した。段階ごとの全体RED/GREENと途中の失敗、追加診断の制約も記録した。

段階6と最終完了条件を満たした。

検証ランナーの親ツールを回収した際の終了コードは143だったが、保存済みの5子グループはすべて終了コード0であり、その後のソース照合と測定値の集計も成功した。親ツールの終了原因は特定していない。各全体テストの成功記録と区別し、`/tmp/elder-webdav-work/stage6-final5-evidence/tool-session-completion.json`に保持した。
